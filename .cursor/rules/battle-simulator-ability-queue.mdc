---
description: 对战模拟器能力队列与触发器约定，修改 BattleSimulator 能力/施放逻辑时必读
globs: **/BattleSimulator/**/*.cs
alwaysApply: false
---

# 能力队列与触发器（BattleSimulator）

修改 `BattleSimulator` 中能力队列、施放队列或触发器相关逻辑时，必须遵守以下约定，避免破坏帧边界与合并语义。

## 双队列语义

- **currentAbilityQueue**：本帧步骤 8 **唯一**处理的对象；内容来自上一帧步骤 9 从 next 移入的条目。
- **nextAbilityQueue**：本帧延后（未到 250ms）、未消耗完 Pending 的条目写回此处；**本帧施放产生的新能力也只加入此处**（见下）。
- **唯一允许「next → current」的位置**：**仅步骤 9**。在步骤 8 开头、循环前或任何其他地方做「把 next 移入 current」都是错误。

## 施放队列（castQueue）触发的能力与步骤 7 顺序

- 步骤 7 对每件施放物品的**顺序**：先读 **Multicast**，再 **InvokeTrigger(Trigger.UseItem, ..., executeImmediate: 委托)**，再若有弹药则 **AmmoRemaining = max(0, AmmoRemaining - 1)**，再 OnCast，最后若有弹药则 **InvokeTrigger(Trigger.Ammo, ...)**。弹药扣减必须在 UseItem 之后，以便「使用一次消耗全部弹药」类物品先用 Immediate 能力减到 0，且 multicast 按满弹计算。
- 步骤 7 调用 `InvokeTrigger(Trigger.UseItem, ...)` 时传入 **executeImmediate** 委托：匹配到的 **Immediate** 能力**不入队**，当场执行（委托内调用 ExecuteOneEffect）；非 Immediate 能力仍加入 nextAbilityQueue。
- 由 castQueue 触发的非 Immediate「使用物品」能力，**必须加入 nextAbilityQueue**。仅调用一次 `InvokeTrigger(Trigger.UseItem, ...)`；「其他物品使用则触发」通过能力 Condition = `And(DifferentFromSource, SameSide)[ + 额外条件 ]` 表达。
- 这样非 Immediate 能力会在下一帧经步骤 9 进入 current，再在下一帧的步骤 8 被处理，即「本帧施放、下一帧生效」。

## 步骤 8 执行过程中引发的新能力

- 执行能力效果（ExecuteOneEffect）时可能触发新能力（如后续扩展的触发器）。
- 新能力入队规则：**只有优先级为 AbilityPriority.Immediate 的加入 currentAbilityQueue**；**其余一律加入 nextAbilityQueue**。
- 因此 `ExecuteOneEffect` 必须接收 `currentAbilityQueue` 与 `nextAbilityQueue` 参数，供触发器调用使用。

## 触发器调用方式与 PendingCount

- 所有触发器均通过**统一**的 `InvokeTrigger(triggerName, causeItem, context, ..., onlyForSideIndex?)` 调用（causeItem 为引起触发的物品引用，BattleStart 传 null；**onlyForSideIndex** 非空时仅遍历该侧，用于即将落败等单侧触发）：**遍历次序**为先 cause 所在侧且先 cause 该件、再同侧其余、再另一侧（无 cause 时按 side0→side1、下标顺序）。**UseItem** 时 context 须传 **InvokeTargetItem = item**（被使用的那件），以便「对被使用的那件物品施加效果」类能力用 SameAsInvokeTarget 正确入队。遍历时对每个物品构建 `ConditionContext`（Source=能力持有者、Item=causeItem）后调用 `condition.Evaluate(ctx)`；若能力有 `InvokeTargetCondition` 且 context 提供 `InvokeTargetItem`，则再以该目标为 Item 求值，不通过则不入队。**只有 Immediate 优先级入 currentAbilityQueue，其余入 nextAbilityQueue**（与步骤 7 施放触发的规则一致）。
- **战斗开始**：步骤 1 只调用 `InvokeTrigger(Trigger.BattleStart, null, null, ...)` 入队，不直接执行；执行在步骤 8（或下一帧步骤 8）进行。
- **UseItem / Freeze / Slow / Haste / Crit / Destroy / Burn / Poison / Shield / Ammo / BattleStart / AboutToLose**：生成/克隆能力时 `EnsureTriggerCondition` 补 Condition：`UseItem` 且 Condition 为 null → `Condition.SameAsSource`；**Ammo**（弹药消耗）默认 `Condition.SameSide`，可用 **additionalCondition: Condition.AmmoDepleted** 限定「仅耗尽当次」；**AboutToLose**（即将落败）默认 `Condition.SameSide`，「首次」由物品状态 **Custom_0** 保证（参考靴里剑：`SourceCustom0IsZero` 时生效，生效后置 1，每件物品每场最多生效一次）；其余触发器（Freeze/Slow/Haste/Crit/Destroy/Burn/Poison/Shield）默认 `Condition.SameSide`；BattleStart → Always。步骤 7 在 `AmmoRemaining = max(0, AmmoRemaining - 1)` 之后若 ammoCap > 0 则调用 `InvokeTrigger(Trigger.Ammo, item, ...)`。**步骤 10** 胜负判定前：若某方 Hp≤0 则对该方调用 `InvokeTrigger(Trigger.AboutToLose, null, null, ..., onlyForSideIndex: 该侧)`，仅执行入队的 Immediate 能力（救生圈治疗等）后重算 Hp 再判胜负；「首次」由物品 Custom_0 限定。**Freeze/Slow/Destroy** 的触发来源：`EffectApplyContextImpl` 不持有回调，而是向模拟器传入的 **EffectAppliedTriggerQueue** 追加 `(TriggerName, SideIndex, ItemIndex)`；`ability.Apply(ctx)` 返回后模拟器统一遍历该队列、按目标解析 `BattleItemState` 并调用 `InvokeTrigger`，Destroy 时在调用后再设 `target.Destroyed = true`。**Freeze/Slow** 为任意物品施加时触发（默认 SameSide 表现为己方）；**Destroy** 与 Slow 同构：Condition 判定施加者，InvokeTargetCondition 判定被摧毁物品，context 传 `InvokeTargetItem`。**Crit** 为任意物品造成暴击时触发；暴击按物品按帧统一判定：步骤 8 循环中，可暴击能力若本帧已为该物品判定过（`item.CritTimeMs == timeMs`）则复用结果；否则掷骰并写入物品状态，**若判定为暴击则在此处、ExecuteOneEffect 之前**调用一次 `InvokeTrigger(Trigger.Crit, item, null, ...)`。ExecuteOneEffect 内不再调用 Crit 触发。
- **条件语义**：评估时 **Source=能力持有者**、**Item=被评估对象**（Condition 时=引起触发的物品，InvokeTargetCondition 时=触发器指向的物品，TargetCondition 时=候选目标）。`Condition` = 引起触发的物品（Item）需满足；`InvokeTargetCondition` = 触发器所指向的物品（Item）需满足；`TargetCondition` = 候选目标（Item）需满足。
- **PendingCount 合并顺序（AddOrMergeAbility）**：
  1. 当 **InvokeTargetItem** 非空（context 提供 invokeTargetSideIndex + invokeTargetItemIndex）时**不合并**，直接新建条目并写入 `InvokeTargetSideIndex`/`InvokeTargetItemIndex`，效果应对该单一目标施加（见 docs/implementation-notes.md「InvokeTarget 与 SameAsInvokeTarget」）。
  2. 否则先在 **currentAbilityQueue** 中查找同 (Owner, AbilityIndex) 且条目无 InvokeTarget 的，找到则只做 `PendingCount += pendingCount`，不新建。
  3. 再在 **nextAbilityQueue** 中查找同上，找到则只做 `PendingCount += pendingCount`，不新建。
  4. 都没有则新建条目：**Immediate → currentAbilityQueue**，**非 Immediate → nextAbilityQueue**。`AbilityQueueEntry` 持有多元组 (Owner, AbilityIndex, PendingCount, 可选 InvokeTargetSideIndex/InvokeTargetItemIndex)。**250ms 节流状态**存于物品（GetLastTriggerMs/SetLastTriggerMs），不存于条目。

## 步骤 8 执行顺序：仅按能力优先级

- 步骤 8 在遍历处理 `currentAbilityQueue` 前，**仅按能力优先级排序**：`AbilityPriority` 枚举值升序（Immediate → Highest → High → Medium → Low → Lowest）；**同优先级按入队顺序**，不再按 (SideIndex, ItemIndex, AbilityIndex) 等二次排序。
- 这样同一帧内触发的多条能力会按「高优先级先于低优先级」执行，同优先级则按入队次序处理（例如裂盾刀裂盾 High 先于姜饼人护盾 Low）。

## 效果数值与光环

- 执行效果时，`IEffectApplyContext.GetResolvedValue` 用于取 Damage、Shield 等。实现（如 `EffectApplyContextImpl`）**必须**传入 `IAuraContext` 调用 `Item.Template.GetInt(key, tier, default, auraContext)`，否则「基础 0 + 光环加成」类物品施放时数值仍为 0。见 `docs/implementation-notes.md` 中「光环（Aura）与属性读取」→「效果数值必须带光环上下文」。
- **战斗内读属性统一带光环**：凡有 (side, item) 的战斗内读（Multicast、CooldownMs、AmmoCap 等）应使用 **`BattleSide.GetItemInt(item.ItemIndex, key, defaultValue)`**，内部用 `BattleAuraContext(this, item)` 调用 `Template.GetInt(..., context)`，保证依赖变量的光环（如「飞行时 +1 多重释放」）正确生效。施放队列 castQueue 为 `List<BattleItemState>`。

## 设计文档对照

- 实现与 `docs/Bazaar Arena 设计文档.md` 中「每一帧的结算顺序」步骤 7、8、9 一致。
- 更多错误案例与修正经验见 `docs/implementation-notes.md` 中「对战模拟器：能力队列的帧边界」「能力优先级与步骤 8 执行顺序」等节。
