---
description: 对战模拟器能力队列与触发器约定，修改 BattleSimulator 能力/施放逻辑时必读
globs: **/BattleSimulator/**/*.cs
alwaysApply: false
---

# 能力队列与触发器（BattleSimulator）

修改 `BattleSimulator` 中能力队列、施放队列或触发器相关逻辑时，必须遵守以下约定，避免破坏帧边界与合并语义。

## 双队列语义

- **currentAbilityQueue**（类型 `AbilityQueueBuckets`）：本帧步骤 8 **唯一**处理的对象；内容来自上一帧步骤 9 **对调 current/next 引用**后，原 next 各优先级桶中的条目。
- **nextAbilityQueue**（类型 `AbilityQueueBuckets`）：本帧延后（未到 250ms）、未消耗完 Pending 的条目写回对应优先级桶；**本帧施放产生的新能力也只加入此处**（见下）。
- **唯一允许「next → current」的位置**：**仅步骤 9**（对调两个 `AbilityQueueBuckets` 引用并 `Clear` 新的 next）。在步骤 8 开头、循环前或任何其他地方做「把 next 并入 current」都是错误。
- 队列桶内元素为**运行时能力引用**（`RuntimeAbilityRef`，绑定 Owner+Ability），而非 `AbilityQueueEntry`；`PendingCount`/`LastTriggerMs` 存于 `AbilityState`。

## 施放队列（castQueue）触发的能力与步骤 7 顺序

- 步骤 7 对每件施放物品的**顺序**：先读 **Multicast**，再 **InvokeTrigger(Trigger.UseItem, ..., executeImmediate: 委托)**，再若有弹药则 **AmmoRemaining = max(0, AmmoRemaining - 1)**，再 OnCast，最后若有弹药则 **InvokeTrigger(Trigger.Ammo, ...)**。弹药扣减必须在 UseItem 之后，以便「使用一次消耗全部弹药」类物品先用 Immediate 能力减到 0，且 multicast 按满弹计算。
- 步骤 7 调用 `InvokeTrigger(Trigger.UseItem, ...)` 时传入 **executeImmediate** 委托：匹配到的 **Immediate** 能力**不入队**，当场执行（委托内调用 ExecuteOneEffect）；非 Immediate 能力仍加入 nextAbilityQueue。
- 由 castQueue 触发的非 Immediate「使用物品」能力，**必须加入 nextAbilityQueue**。仅调用一次 `InvokeTrigger(Trigger.UseItem, ...)`；「其他物品使用则触发」通过能力 Condition = `And(DifferentFromCaster, SameSide)[ + 额外条件 ]` 表达。
- 这样非 Immediate 能力会在下一帧经步骤 9 进入 current，再在下一帧的步骤 8 被处理，即「本帧施放、下一帧生效」。

## 步骤 8 执行过程中引发的新能力

- 执行能力效果（ExecuteOneEffect）时可能触发新能力（如后续扩展的触发器）。
- 新能力入队规则：**只有优先级为 AbilityPriority.Immediate 的加入 currentAbilityQueue**；**其余一律加入 nextAbilityQueue**。
- 因此 `ExecuteOneEffect` 必须接收 `currentAbilityQueue` 与 `nextAbilityQueue` 参数，供触发器调用使用。

## 触发器调用方式与 PendingCount

- 所有触发器均通过**统一**的 `InvokeTrigger(triggerName, causeItem, invokeTargetItem|invokeTargetItems, invokeCount, executeImmediate)` 调用。匹配顺序按 **`SessionTables.AbilitiesByTrigger[triggerName]`** 直接遍历（不再按 cause 侧/同侧/异侧二次排序）。遍历时对每个能力构建 `BattleContext`（`Caster`=能力持有者、`Source`=causeItem 或缺省为 Caster、`Item`=Source、`InvokeTarget`=传入目标），再按能力的 `TriggerEntries` 判定 `entry.Condition`；命中后按优先级入队（**Immediate 入 current，其余入 next**）。`UseItem/UseOtherItem` 时应传 `invokeTargetItem = 被使用物品`，保证 `SameAsInvokeTarget` 语义可用。
- **战斗开始**：步骤 1 只调用 `InvokeTrigger(Trigger.BattleStart, null, null, ...)` 入队，不直接执行；执行在步骤 8（或下一帧步骤 8）进行。
- **UseItem / UseOtherItem / Freeze / Slow / Haste / Crit / Destroy / Burn / Poison / Shield / Ammo / BattleStart / AboutToLose**：能力定义在注册阶段由 `ItemDatabase.NormalizeAbilities` 规范化：保证 `TriggerEntries` 非空、默认触发为 `UseItem + SameAsCaster`，并为每个 entry 补齐空条件（`UseItem→SameAsCaster`，`UseOtherItem→SameSide & DifferentFromCaster`，其余多为 `SameSide`）。`Override(trigger: ...)` 不会重置 `TargetCondition`。步骤 7 在扣减弹药后触发 `Trigger.Ammo`。生命判定前统一触发 `Trigger.AboutToLose`，随后仅排空 current 队列中的能力以完成救场结算。`Freeze/Slow/Destroy` 由 `BattleContext.EffectApply` 内部在施加时直接 `InvokeTrigger`（Destroy 触发后再标记 `Destroyed`）。暴击为“每物品每帧一次判定”，判定成功后在效果执行前触发一次 `Trigger.Crit`。
- **条件语义**：**InvokeTrigger** 内 **TriggerEntry.Condition** 求值时：`Caster`=能力持有者，`Item`=引起触发者（causeItem；无则 Caster），`Source` 同 `Item`（引起触发者），`InvokeTarget`=触发器指向物品（若有）。效果层 **TargetCondition**/**additionalTargetCondition** 筛选施加目标时 `Item`=候选目标（`Source` 通常也设为候选目标）；需「仅对触发指向的那一件」时用 **SameAsInvokeTarget** 等（见 **project-conventions** 硬切约定）。
- **PendingCount 合并顺序（AddOrMergeAbility）**：
  1. 运行态状态键必须是**Owner 维度能力引用**（如 `RuntimeAbilityRef.Id`），不能只用 `AbilityId`（模板能力共享时会 owner 覆盖）。
  2. 命中同一运行时能力时，`PendingCount += pendingCount`；仅当从 0 变为正数时入队到对应优先级桶。
  3. 当存在 `InvokeTargetItem` 时，将目标追加到 `AbilityState.InvokeTargets`（按次序消费），不可覆盖单一 index。
  4. 入桶规则不变：**Immediate → current**，其余 → **next**。**250ms 节流状态**存于 `AbilityState.LastTriggerMs`。

## 步骤 8 执行顺序：仅按能力优先级

- 步骤 8 遍历处理 **current** 的 **6 个优先级桶**（`AbilityQueueBuckets`）：`AbilityPriority` 枚举值升序（Immediate → Highest → High → Medium → Low → Lowest）；**同优先级按入队顺序**，不再按 (SideIndex, ItemIndex, AbilityIndex) 等二次排序。`AddOrMergeAbility` 入队时直接写入对应桶；与原先先整表分桶再遍历的次序一致。
- 这样同一帧内触发的多条能力会按「高优先级先于低优先级」执行，同优先级则按入队次序处理（例如裂盾刀裂盾 High 先于姜饼人护盾 Low）。

## 效果数值与光环

- 执行效果时，`Apply` 通过 `ctx.GetItemInt` / `ctx.GetItemInt(ctx.Caster, ability.ValueKey!.Value)` 读取运行时属性并叠加光环（`BattleContext.GetItemInt`）。
- **规则判定与效果读数一致**：冷却是否跑满、弹药上限、暴击 CanCrit、`Formula.SideSelect` 等与光环相关的判定，须用 **`BattleState.GetItemInt(item, key)`**，勿在战斗主路径上仅用 **`BattleSide.GetItemInt`** / **`GetAttribute`** 做阈值判断（详见 **`docs/implementation-notes.md`**「规则读数统一」）。
- **默认值回退不可丢**：`BattleSide.GetItemInt` / `BattleContext.GetItemInt` 在“值为 0 且 defaultValue 非 0”时的语义须保留（如 Multicast、TargetCount 默认 1）。

## BuildSide 与模板使用边界

- 战斗过程仅允许读取 `ItemTemplate.Name`（日志）；其他战斗读数统一来自 `ItemState` 与会话索引（abilities/auras）。
- `ItemState.Abilities` 可直接引用模板能力定义（不 clone），但 `AbilityState` 必须按 Owner 维度拆分（如 `RuntimeAbilityRef`），避免共享 `AbilityId` 造成状态串扰。

## 并行调用与实例状态

- `BattleSimulator.Run` 可被 **GreedyDeckFinder**（`BattleEvaluator` + `Parallel.For`）等场景 **多线程同时调用**；修改 `Run` 或 `PrepareDeck` 时 **勿** 在类型实例字段上存放 **仅本场对局有效** 且会被下一场覆盖的状态（例如本场 `BattleSessionTables`），否则并发 `Run` 会串场，表现为光环/能力 id 与 `BattleState` 不一致甚至越界。
- 可跨场共享的缓存（如 `PrepareDeck` 结果）若被并行访问，须 **加锁或使用并发集合**，保证线程安全。
- Greedy 侧架构与经验见 **`.cursor/rules/greedy-deck-finder.mdc`**「架构与 `BattleSimulator` 线程安全」与 **`docs/greedy-deck-finder-performance-notes.md`** §3.6。

## 设计文档对照

- 实现与 `docs/Bazaar Arena 设计文档.md` 中「每一帧的结算顺序」步骤 7、8、9 一致。
- 更多错误案例与修正经验见 `docs/implementation-notes.md` 中「对战模拟器：能力队列的帧边界」「能力优先级与步骤 8 执行顺序」等节。
