---
description: 禁止用绕过方式掩盖问题，须修复根因
alwaysApply: true
---

# 禁止掩耳盗铃式修改

- **不得**在代码中加入「绕过真实逻辑以通过测试」的补救逻辑，例如：
  - 在模拟器/引擎中按**物品名**、**ID** 等硬编码强制入队、强制通过条件；
  - 放宽或跳过条件求值、断言，使测试通过但行为仍错误；
  - 用 try-catch 吞掉异常后返回默认值而不修复根因。
- **须**定位并修复**根因**（条件求值、克隆、上下文传递等）；测试未通过时在文档中标注「未通过测试」并写明原因，不放松断言、不添加按名称/特例的绕过代码。
