---
description: 物品与效果设计经验：触发器、数值、效果委托、光环、Desc 与战斗内修改
alwaysApply: false
globs: "**/ItemDatabase/**","**/Core/BattleContext*.cs","**/Core/Apply.cs","**/Core/Ability*.cs","**/Core/ItemTemplate.cs","**/Core/Aura*.cs","**/Core/AttributeLogNames.cs","**/Core/Condition.cs","**/Core/Formula.cs","**/Core/Key.cs"
---

# 物品设计经验

## 物品模板（ItemTemplate）

### 基础与注册

- **基础字段**：`Name`、`Desc`、`Tags`。Tags 使用 `Tag.Weapon`、`Tag.Friend`、`Tag.Tool`、`Tag.Apparel`、`Tag.Food`、`Tag.Tech`、`Tag.Property`、`Tag.Vehicle`、`Tag.Relic`、`Tag.Dragon`、`Tag.Drone`、`Tag.Toy` 等。**尺寸 Tag**（`Tag.Small`、`Tag.Medium`、`Tag.Large`）与**类型/机制 DerivedTag**（Damage、Burn、Poison、Heal、Shield、Regen、Charge、Freeze、Slow、Haste、Reload、Repair、Destroy、StartFlying、StopFlying、Crit、Cooldown、Ammo）由 **Register** 内 **EnsureTypeTags** 按 **AbilityType** 与 `Condition.SameAsCaster` 光环等自动添加，无需手写。
- **MinTier、Size**：**不在物品工厂中设置**；在 **RegisterAll** 中通过 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`Register` 时写入模板。

### 数值与时间

- **数值**：按等级用 `IntOrByTier`，单值或列表：`Damage = [5,10,15,20]`、`Custom_0 = [1,2,3,4]`。属性名与 **Key** 一致（见 `Core/Key.cs`），物品定义与能力侧统一用 **Key.Damage**、**Key.Custom_0** 等，不用 `nameof(ItemTemplate.xxx)`。
- **冷却**：用 `Cooldown = 5.0`（秒）；按 tier 不同冷却时用 `Cooldown = [9.0, 8.0, 7.0]`。内部由模板转为毫秒存储。
- **时间一律用秒**：物品定义中表示时间用秒（`Freeze = [3.0, 4.0, 5.0]`、`Charge = 1.0`、`Slow = 1.0`、`Haste = 2.0` 等），内部由 **SecondsOrByTier.ToMilliseconds()** 转为毫秒。多目标数量：`FreezeTargetCount`、`SlowTargetCount`、`HasteTargetCount`、`ChargeTargetCount`、`RepairTargetCount`、`DestroyTargetCount` 默认 1，为 1 时可省略。

### 描述（Desc）与占位符

- **占位符**：`{Damage}`、`{Cooldown}`、`{Burn}`、`{Poison}`、`{Custom_0}`、`{+Custom_0%}` 等；`ItemDescHelper` 用正则解析。`{Cooldown}` 映射为 `CooldownMs` 并按秒显示；`{ChargeSeconds}`、`{FreezeSeconds}`、`{SlowSeconds}`、`{HasteSeconds}` 映射为对应毫秒字段并按秒显示。
- **冻结/减速/加速 Desc**：仅当能力为**默认选取目标**时，采用「X **{XTargetCount} 件物品 {XSeconds} 秒**」形式（如「冻结 {FreezeTargetCount} 件物品 {FreezeSeconds} 秒」）。有**确定目标**（如相邻、右侧）时保留「加速相邻物品 {HasteSeconds} 秒」「使之加速 {HasteSeconds} 秒」等写法。

### 光环（Auras）

- **结构**：`Auras` 为 `List<AuraDefinition>`。每条含：
  - **AttributeName**：作用的属性名，用 **Key.CritRatePercent**、**Key.Damage** 等。
  - **Condition**：对该目标生效的条件，**默认 SameAsCaster**；仅当**非** SameAsCaster 时才显式写（如 `Condition.AdjacentToCaster`、`Condition.WithTag(Tag.Weapon)`）。作用自身时不必写 `Condition = Condition.SameAsCaster`。
  - **Condition**：`AuraDefinition.Condition` 同时承担“目标筛选 + 提供者限制”。默认 `Condition.SameAsCaster`；若需限制提供者，把条件并入同一表达式（如 `Condition.SameAsCaster & Condition.InFlight`）。
  - **Value**：`Formula?`，由 `Formula.Evaluate` 计算。如 `Formula.Source(Key.Custom_0)`、`Formula.Opp(BattleSide.KeyPoison)`、`Formula.Source(Key.Custom_0) * Formula.Count(Condition.SameSide & Condition.WithTag(Tag.Friend))`。`FixedValueKey` / `FixedValueFormula` / `PercentValueKey` 已废弃，统一为 **Value + Percent**。
  - **Percent**：`bool`，默认 false。为 true 时公式结果为百分比加成（+N% 加算），如利爪暴击伤害 +100%。
- **公式**：使用 **Formula.Source(key)**、**Formula.Side(key)**、**Formula.Opp(key)**、**Formula.Count(condition)** 与 `+`/`-`/`*`、一元负号、`int * Formula` 组合；可用 **RatioUtil.PercentFloor(Formula, percent)**。战斗内读属性统一走 **`BattleState.GetItemInt(item, key)`** 或 **`ctx.GetItemInt(item, key)`**，按当前实现会合并光环。

### 可复写属性与其它

- **OverridableAttributes**：`Dictionary<int, IntOrByTier>?`，卡组内可复写属性及按 tier 默认值。键用 **Key.Custom_1**、**Key.StashParameter** 等。**约定：仅在此处写一次默认值**，不要在模板上再写同名属性（如只写 `OverridableAttributes = { [Key.Custom_1] = [5, 10, 15, 20] }`，不写 `Custom_1 = [5, 10, 15, 20]`）；**Register** 会将 OverridableAttributes 的默认值同步到模板，供战斗内公式与 Desc 使用。详见 `docs/implementation-notes.md`「OverridableAttributes 与 Aura Condition 约定」。
- **局外成长（光环 + 可覆盖变量）**：若效果为「局外某条件（如购买水系、赢得战斗）时此物品某属性提高 X/Y/Z/W」，不实现局外触发，但可用**基础数值（若有）+ 光环公式 + OverridableAttributes** 描述：**Custom_0** 写模板上（每档提高量等），**Custom_1** 仅写 **OverridableAttributes**（局外可覆盖，已购买数量常用默认 5/10/15/20）；光环 `Value = Formula.Source(Key.Custom_0) * Formula.Source(Key.Custom_1)`。参考 **珊瑚**（治疗）、**珊瑚护甲**（护盾）、**狼筅**。
- **StashParameter**：用于如储物箱「小型物品数 + StashParameter」类公式时，默认值只写在 **OverridableAttributes** 中即可。
- **弹药**：`AmmoCap`（0 表示无弹药）；使用后扣减 `AmmoRemaining`，日志可显示剩余弹药。
- **战斗内状态**：战斗内可变值存于 `ItemState.Attributes`；模板定义按引用复用。运行时改值用 `item.SetAttribute(...)` / `item.GetAttribute(...)`，不要依赖“战斗内模板克隆”。

---

## 触发器与能力

### 触发器

- **命名**：`Trigger.UseItem`、`Trigger.BattleStart`、`Trigger.Crit`、`Trigger.Freeze`、`Trigger.Slow`、`Trigger.Destroy` 等（不带 On 前缀）。
- **UseItem**：冷却转好后施放，能力入队，按 250ms 间隔与优先级结算。默认 **Condition = SameAsCaster**（即「此物品被使用时」）。
- **「当其他某类物品被使用」**：优先用 `UseOtherItem`，并写 **condition: Condition.SameSide & Condition.DifferentFromCaster & Condition.WithTag(Tag.xxx)**（如「使用工具时」）。不要把该语义写成 `UseItem + additionalCondition`。
- **「当右侧/相邻物品被使用」**：用 **condition: Condition.DifferentFromCaster & Condition.SameSide & Condition.RightOfCaster**（或 `Condition.AdjacentToCaster` 等）。
- **BattleStart / Crit / Destroy**：默认 Condition 为 SameSide；需「仅自身」时写 **condition: Condition.SameAsCaster**。

### Ability 工厂与 Key

- **无参属性**（`Core/Ability.cs`）：**Damage**、**Shield**、**Heal**、**Burn**、**Poison**、**Charge**、**Haste**、**Slow**、**Freeze**、**StartFlying**、**StopFlying**、**Destroy**、**Repair**。返回带默认值的 `AbilityDefinition`。
- **方法**：**AddAttribute(attributeName, amountKey?)**、**ReduceAttribute(attributeName, amountKey?)**，需指定属性 key（如 **Key.Damage**、**Key.Shield**）。
- **数值/属性 key**：一律用 **Key.***（`Core/Key.cs`），如 Key.Damage、Key.Custom_0、Key.CooldownMs、Key.InFlight 等。
- **定制**：统一用 **Ability.xxx.Override(...)**，参数顺序见 **ability-override-format.mdc**。多效果或特殊 Condition 可用 `new AbilityDefinition { ..., Apply = Apply.XXX, ... }`（**`Core/Apply.cs`** 内 **`Action<BattleContext, AbilityDefinition>`**），但优先用工厂 + Override。

### 飞行（InFlight）

- **飞行为属性**：运行时状态存于 `ItemState.Attributes` 的 `Key.InFlight`。`Ability.StartFlying` 固定 `valueKey: Key.Custom_0`，模板需提供正值（常见 `Custom_0 = 1`）；`Ability.StopFlying` 用于结束飞行。

### 能力语义速查

- **Condition**：引起触发的物品需满足；UseItem 默认 SameAsCaster。
- **TargetCondition**：效果施加给谁；AddAttribute 默认 SameSide，ReduceAttribute 默认 DifferentSide。
- **「此物品获得 X」**：TargetCondition 须为 **SameAsCaster**，Condition 描述谁触发（如 `AdjacentToCaster`）。
- **触发器指向目标**：效果选「被触发指向的那一件」用 **`additionalTargetCondition: Condition.SameAsInvokeTarget`**。若语义须判断 **InvokeTarget** 的阵营/标签/飞行（与 **Source** 无关），在 **TriggerEntry.Condition** 中用 **`Condition.InvokeTargetSameSide`**、**InvokeTargetDifferentSide**、**InvokeTargetSameAsCaster**、**InvokeTargetWithTag**、**InvokeTargetInFlight** 等（**`AbilityDefinition.Override` 已无 `invokeTargetCondition` 参数**）。**`Ability.Damage`** 不读 **TargetCondition**，「仅当 InvokeTarget 满足 X 才造成伤害」须把门控写在 **condition**（如牵引光束第二段：**SameAsCaster & (InvokeTargetWithTag(Large) | InvokeTargetInFlight)**）。

---

## 效果与数值

- **Apply**：**`AbilityDefinition.Apply`** 为 **`Action<BattleContext, AbilityDefinition>?`**；预定义委托在 **`Core/Apply.cs`**。数值统一通过 `ctx.GetItemInt(ctx.Caster, ability.ValueKey!.Value)` 读取。
- **ValueKey**：能力不再有 `Value`/`ctx.Value`/`Override(value: ...)`；常量效果请落到模板字段（常见 `Custom_x`）并通过 `valueKey` 读取。
- **AddAttribute / ReduceAttribute**：目标默认己方/敌方；用 **additionalTargetCondition** 收窄（如 `Condition.WithTag(Tag.Weapon)`），或 **targetCondition** 完全自定义。定义时在默认目标上**仅追加条件**时优先用 **additionalTargetCondition**，避免写完整 targetCondition（如「己方且飞行」用 `additionalTargetCondition: Condition.InFlight`）。
- **SetAttributeOnCasterSide**：对己方目标将属性设为某值（如 StopFlying 设 Key.InFlight 为 0），对应 **`Apply.StopFlying`**（**`Core/Apply.cs`**）。
- **摧毁**：**Ability.Destroy** 默认 **targetCondition** 为 **敌方未摧毁**（**DifferentSide**，工厂 **WithNotDestroyedTarget**）。摧毁己方「右侧第一件未摧毁」等须 **完整 targetCondition**（如 **`SameSide & ~Destroyed & FirstNonDestroyedRightOfCaster`**），不可在默认敌方下仅叠 **additionalTargetCondition**。**Override(trigger: …)** 不会按触发器覆盖 **TargetCondition**；**Destroy** 改 **Ammo** 等仍保留工厂默认 **DifferentSide**。先 **InvokeTrigger(Destroy)** 再标记 **Destroyed**。
- **物品类型 Tag**：由 **EnsureTypeTags** 按 Damage/Burn/Poison/Heal/Shield/Regen 及 `Condition.SameAsCaster` 光环自动补充，无需手写。

---

## 战斗内修改与 IntOrByTier

- **IntOrByTier.Add(delta)**：举重手套等「武器伤害 +N」用 **Ability.AddAttribute(Key.Damage).Override(additionalTargetCondition: Condition.WithTag(Tag.Weapon), priority: ...)**，内部执行 `Template.Damage = Template.Damage.Add(value)`。
- **Damage 等 getter**：返回完整按等级列表，以支持 `.Add(delta)` 后 set 回。

---

## Tooltip 与 UI

- **冷却行**：无冷却（CooldownMs 为 0）时不显示「冷却时间」行。
- **效果关键词**：伤害、灼烧、暴击等仅着色；加粗用于物品名与占位符替换出的数值。
- **Desc 分行**：可用分号（`;` 或 `；`）分段，Tooltip 按段拆行。

---

## 灼烧衰减

- 每 500ms：衰减量 = 当前灼烧的 3%（`RatioUtil.PercentFloor(burn, 3)`，至少 1）；`victim.Burn = Math.Max(0, victim.Burn - decay)`。

---

## 新增公共物品流程

1. 按尺寸放入 **CommonSmall.cs**、**CommonMedium.cs** 或 **CommonLarge.cs**，写工厂方法：Name、Desc、Tags、数值属性、**Abilities**（不设 MinTier、Size）。能力优先 **Ability.Damage**、**Ability.Shield** 等 + **.Override(...)**；加/减属性用 **Ability.AddAttribute(Key.xxx)** / **Ability.ReduceAttribute(Key.xxx)**。光环用 **AuraDefinition** 的 **Value**（Formula）、**Percent**，AttributeName 用 **Key.***。
2. 若有卡组可复写属性，设 **OverridableAttributes**（键用 **Key.StashParameter** 等）。
3. 在对应 **RegisterAll** 中设 `db.DefaultSize`、再按档位设 `db.DefaultMinTier`，然后 `db.Register(XXX())`。
4. 占位符由 **ItemDescHelper** 解析；时间用秒、内部 ToMilliseconds。
