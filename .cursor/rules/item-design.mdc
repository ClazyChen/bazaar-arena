---
description: 物品与效果设计经验：触发器、数值、效果委托、光环、Tooltip 与战斗内修改
alwaysApply: false
globs: "**/ItemDatabase/**","**/Core/Effect*.cs","**/Core/Ability*.cs","**/Core/ItemTemplate.cs","**/Core/Aura*.cs","**/Core/IEffectApplyContext.cs","**/Core/Condition.cs"
---

# 物品设计经验

## 物品模板（ItemTemplate）

- **基础**：`Name`、`Desc`、`Tags`（用 `Tag.Weapon`、`Tag.Friend`、`Tag.Tool`、`Tag.Apparel`、`Tag.Food`、`Tag.Tech`、`Tag.Property`、`Tag.Vehicle`、`Tag.Relic`、`Tag.Dragon`、`Tag.Drone`、`Tag.Toy` 等）。**MinTier、Size 不在物品工厂中设置**，由 **RegisterAll** 中 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`Register` 时写入模板（见 **project-conventions.mdc**、**implementation-notes.md** 物品定义简化一节）。
- **数值**：按等级用 `IntOrByTier`，支持单值或列表：`Damage = [5,10,15,20]`、`Burn = [6,9,12,15]`、`Poison = [1,2,3,4]`、`Custom_0 = [1,2,3,4]`。冷却用 `Cooldown = 5.0`（秒），无冷却则不设或为 0；**按 tier 不同冷却**时用 `CooldownMs = [9000, 8000, 7000]`（毫秒，与 MinTier 对应）。
- **时间一律用秒**：物品定义中表示时间时用秒以保证可读性（见 **project-conventions.mdc**）。充能用 `ChargeSeconds = 1.0`；冻结用 `FreezeSeconds`、`FreezeTargetCount`；减速用 `SlowSeconds`、`SlowTargetCount`；加速用 `HasteSeconds`、`HasteTargetCount`（目标数，默认 1），内部由模板 **SecondsOrByTier.ToMilliseconds()** 转为毫秒存储。多目标效果的目标选择：冻结/减速/充能/加速的**目标池为有冷却**物品，可选 `AbilityDefinition.TargetCondition`（默认冻结/减速为 DifferentSide，充能/加速为 SameSide）；**修复**的目标池为**已摧毁**物品，无时长，用 `RepairTargetCount`（默认 1），默认 TargetCondition 为 SameSide。
- **描述占位符**：`Desc` 中可用 `{Damage}`、`{Cooldown}`、`{Burn}`、`{Poison}`、`{Custom_0}` 等；支持前缀/后缀格式 `{+Custom_0%}`（大括号内非字母数字下划线的连续字符为前缀/后缀，与数值一并替换并参与加粗与 tier 着色）。`ItemDescHelper` 用正则动态解析，无需单独登记 key；`{Cooldown}` 映射为 `CooldownMs` 并按秒显示。
- **战斗内模板**：每场战斗 `BuildSide` 时每个槽位用卡组 template 克隆一份；`BattleItemState.Template` 即该克隆，可直接修改（如 `Template.Damage = Template.Damage.Add(delta)`），无需额外字段。
- **光环**：`Auras` 为 `List<AuraDefinition>`；每条含 `AttributeName`、`Condition`（委托式）、`FixedValueKey`、`PercentValueKey`、可选 `FixedValueFormula`。公式名用 **`Formula.SmallCountStash`**、**`Formula.SourceDamage`**（来源物品 Damage 含光环，用于如 Burn += 自身 Damage）等常量（`Core/Formula.cs`），不可手写字符串；公式实现集中在 `BattleSimulator/AuraFormulaEvaluator.cs`，不在 BattleAuraContext 内堆分支。光环条件除 `SameAsSource`、`AdjacentToSource`、`WithTag` 外，可用 **`Condition.InFlight`**（提供光环的物品处于飞行状态，依赖 `ConditionContext.SourceInFlight`）。AttributeName/Key 用 `nameof(ItemTemplate.xxx)`。仅在战斗内读取属性且传入 `IAuraContext` 时生效；公式为 `(基础 + Σ固定) × (1 + Σ百分比/100)`，被摧毁物品不提供光环。战斗内读任意属性应走 **`BattleSide.GetItemInt(itemIndex, key, default)`**，统一带光环上下文。
- **可复写属性**：`OverridableAttributes` 为 `Dictionary<string, IntOrByTier>?`，表示卡组内可复写的属性及按 tier 的默认值；默认 null。拖入卡组或改 Tier 时用此初始化/更新槽位的 Overrides。如废品场长枪的 `StashParameter` 可复写，默认 `[1,2,3,4]`。
- **StashParameter**：`IntOrByTier`，用于如废品场长枪「小型物品数 + StashParameter」类公式，默认 0。
- **弹药**：`AmmoCap`（IntOrByTier，0 表示无弹药）；有弹药时使用后扣减 `AmmoRemaining`，日志可通过 `IBattleLogSink.OnCast(..., ammoRemainingAfter)` 显示剩余弹药。

## 触发器与能力

- **使用物品**：冷却转好后施放，能力进入队列，按 250ms 间隔与优先级结算；`TriggerName = Trigger.UseItem`。**Priority 默认 Medium**（`AbilityDefinition.Priority` 默认值）；仅非 Medium 时显式写 `Priority = AbilityPriority.High` 等。
- **Ability 工厂**：单效果优先用 **Core/Ability.cs** 的工厂方法：**Damage()**、**Shield()**、**Heal()**、**Burn()**、**Poison()**（默认 trigger=UseItem，可选 priority、condition、additionalCondition、invokeTargetCondition）；**Haste()**、**Slow()**、**Freeze()**（同上，另可选 targetCondition、additionalTargetCondition）。默认 trigger 为 UseItem；condition 覆盖触发器默认（UseItem→SameAsSource，其他→SameSide）；**additionalCondition** 仅作工厂参数，在工厂内与默认 condition 做 And 后写入 `AbilityDefinition.Condition`；**invokeTargetCondition** 为触发器所指向物品需满足的条件（如 Slow 时被减速物品），默认 null。多效果或特殊 Condition 仍用 `new AbilityDefinition { ... }`。
- **使用物品时「其他物品使用则触发」**：用 `Trigger.UseItem` + `Condition = And(DifferentFromSource, SameSide)[ + 额外条件 ]`（如 `Condition.WithTag(Tag.Tool)`、`Condition.UsedItemRightOfSource`）。不再使用 `Trigger.UseOtherItem`。见 **battle-simulator-ability-queue.mdc**。
- **战斗开始**：第 0 帧触发一次；`TriggerName = Trigger.BattleStart`。模拟器通过统一 `InvokeTrigger` 入队，条件用 `Condition.Evaluate(ConditionContext)` 评估。
- **造成暴击时**：己方某次可暴击效果掷出暴击后触发；`TriggerName = Trigger.OnCrit`。来源=暴击施放者，候选=己方所有带此能力的物品；`EnsureTriggerCondition` 默认 `Condition.SameSide`。
- **摧毁物品时**：己方某物品被摧毁时触发；`TriggerName = Trigger.OnDestroy`。来源=造成本次摧毁的物品，候选=持有该能力的物品；**须在将目标标记为 Destroyed 之前**调用 `InvokeTrigger`，以便被毁物品自身能力仍可触发。`TriggerInvokeContext` 传入 `DestroyedItemTemplate`、`DestroyedItemInFlight`；`ConditionContext` 同步填入，供如 `Condition.DestroyedTargetIsLargeOrInFlight` 使用。`EnsureTriggerCondition` 默认 `Condition.SameSide`；能力上常用 `Condition.SameAsSource` 表示「仅造成这次摧毁的物品」触发。
- **能力**：`AbilityDefinition` 含 `TriggerName`、`Priority`、`Condition`（引起触发的物品需满足；null 时由触发器默认）、`InvokeTargetCondition`（触发器所指向物品需满足，如 Slow 时被减速物品，默认 null）、`TargetCondition`（效果选目标时目标需满足，用于冻结/减速/充能/加速等）、`Effects`（`EffectDefinition` 列表）。每条效果有 `Value`、可选 `ValueResolver`、可选 `ValueKey`、`ApplyCritMultiplier`、`Apply` 委托。

## 效果与数值（委托驱动）

- **效果应用**：`EffectDefinition.Apply` 为 `Action<IEffectApplyContext>`；模拟器构建 `IEffectApplyContext` 实现后调用 `eff.Apply(ctx)`。预定义效果在 **Core/Effect.cs** 内设置 `Apply`；只读效果在委托内用 `ctx.GetResolvedValue(nameof(ItemTemplate.XXX), ...)` 取值。**GetResolvedValue 必须带光环上下文**：实现内应使用 `Item.Template.GetInt(key, tier, default, auraContext)`（如 `EffectApplyContextImpl` 创建 `BattleAuraContext(Side, ItemIndex)` 传入），否则「基础 0 + 光环」类物品施放时数值仍为 0。多目标效果的目标选择由 `TargetCondition` 经 `ctx.TargetCondition` 传入。
- **ValueKey**：指定了 ValueKey 的效果（如 `Effect.AddAttribute`、`Effect.ReduceAttribute`）由模拟器解析数值并填入 `ctx.Value`；模拟器仅在 `eff.ValueKey != null` 时解析。
- **暴击**：是否乘暴击由物品六字段（Damage/Burn/Poison/Heal/Shield/Regen 任一 > 0）与 `eff.ApplyCritMultiplier` 决定。日志「（暴击）」仅对可暴击效果显示：`LogEffect(..., showCrit: ctx.IsCrit)` 用于伤害/灼烧等，Charge/Freeze/Slow 等传 `showCrit: false`。
- **AddAttribute / ReduceAttribute**：己方加属性、敌方减属性统一用 **Effect.AddAttribute** / **Effect.ReduceAttribute**，不再写自定义 Effect 或专用 API。默认 `amountKey = Custom_0`、`targetCondition = SameAsSource`；仅需自身+Custom_0 时省略后两参，如 `Effect.AddAttribute(nameof(ItemTemplate.Poison))`；需指定目标时用命名参数，如 `targetCondition: Condition.WithTag(Tag.Weapon)`、`targetCondition: Condition.IsShieldItem`。见 `docs/implementation-notes.md` 中「AddAttribute / ReduceAttribute 与统一属性增减」。
- **扩展效果**：在 `Core/Effect.cs` 新增静态 `EffectDefinition` 并设置 `Apply` 与可选 `ApplyCritMultiplier`、`ValueKey` 即可，无需单独 Handler 文件。定向效果（护盾降低、伤害提高）日志格式为「效果名 数值 →[目标]」；优先用 AddAttribute/ReduceAttribute 表达，无法表达时再在物品定义内 `new EffectDefinition { ... }`。
- **飞行（In Flight）**：`BattleItemState.InFlight` 为运行时状态，战斗开始为 false。「开始飞行」用 **`Effect.StartFlying`**（设置 `InFlight = true` 并记日志「开始飞行」）；若施放者**已在飞行**则不重复结算、不记日志（幂等）。Tooltip 与日志中「飞行」着色与护盾同色（`EffectKeywordFormatting`）。
- **摧毁**：**`Effect.DestroyNextItemToRightOfCaster`** 摧毁己方施放者**右侧下一件未摧毁**物品（从 ItemIndex+1 起向右扫描）；由实现方先调用 OnDestroy 触发器、再标记 `Destroyed = true`。牵引光束等可在此基础上用 `Trigger.OnDestroy` + `Condition.SameAsSource` 造成伤害，用 `Condition.DestroyedTargetIsLargeOrInFlight` 判定被毁目标为大型或飞行时再造成伤害。日志中「摧毁」「修复」不显示数值（仅显示效果名与 extraSuffix，如「摧毁 →[物品名]」）；「摧毁」颜色 rgb(255,50,120)，见 **data-and-logging.mdc**。
- **物品类型 Tag**：判断「是否为护盾/伤害/灼烧物品」「是否可暴击」时使用模板的 `Tags`（`Tag.Shield`、`Tag.Damage` 等）。注册时由 `ItemDatabase.Register` 的 `EnsureTypeTags` 自动补充：① 模板任一档位下六类属性（Damage/Burn/Poison/Heal/Shield/Regen）> 0 则打对应 Tag；② 光环条件为 **SameAsSource** 且 **AttributeName** 为上述六类之一也打对应 Tag（如仅由光环提供伤害的物品）。无需在物品定义中手写。见 `docs/implementation-notes.md` 中「物品类型 Tag」。

## 战斗内修改与 IntOrByTier

- **IntOrByTier.Add(delta)**：每个 tier 的值都加 delta，返回新的 `IntOrByTier`；举重手套等「武器伤害 +N」用 **Effect.AddAttribute(nameof(ItemTemplate.Damage), targetCondition: Condition.WithTag(Tag.Weapon))**，内部对己方满足条件的物品执行 `Template.Damage = Template.Damage.Add(value)`。
- **Damage 的 getter**：模板的 `Damage` 属性 get 返回完整按等级列表（`GetIntOrByTier(KeyDamage)`），以便支持 `.Add(delta)` 后 set 回；不要用仅返回单值的 getter 做此类修改。

## Tooltip 与 UI 表现

- **冷却行**：若物品无冷却（`CooldownMs` 为 0），Tooltip **不显示**「冷却时间」那一行（卡组内与物品池均按此判断）。
- **效果关键词**：`EffectKeywordFormatting` 中「伤害」「灼烧」「暴击」「暴击率」「暴击伤害」等仅**着色**、不加粗；加粗仅用于物品名称和占位符替换出的数值区段。颜色与战斗日志一致（如伤害/暴击系 rgb(0xf5,0x50,0x3d)，灼烧 rgb(255,159,69)，剧毒 rgb(14,190,79)）。
- **暴击伤害**：ItemTemplate 有 `CritDamagePercent`，默认 200（2 倍暴击）；暴击时倍率 = CritDamagePercent/100，作用于所有可暴击效果。光环可对自身 `SameAsSource` 增加 CritDamagePercent 百分比（如利爪 +100% → 400 即 4 倍）。
- **Desc 分行**：Desc 中可用分号（`;` 或 `；`）分段，Tooltip 会按段拆成多行显示。

## 灼烧衰减

- 每 500ms tick 时：衰减量 = 当前灼烧的 3%，用 `RatioUtil.PercentFloor(burn, 3)`（至少为 1）；`victim.Burn = Math.Max(0, victim.Burn - decay)`。这样灼烧 1 会衰减 1 变为 0。

## 新增公共物品流程

1. 按尺寸放入 `ItemDatabase/CommonSmall.cs`、`CommonMedium.cs` 或 **`CommonLarge.cs`**，写工厂方法，设置 Name、Desc、**Tags**、数值属性、**Abilities**（**不**设置 MinTier、Size）；若为纯光环或「基础 0 + 光环」可设 `Auras`，公式用 `FixedValueFormula = Formula.SmallCountStash` 等（见 `Core/Formula.cs`），不手写字符串。能力优先用 **Ability.Damage()**、**Ability.Shield()**、**Ability.Heal()**、**Ability.Burn()**、**Ability.Poison()**、**Ability.Haste(...)**、**Ability.Slow(...)**、**Ability.Freeze(...)**（默认 trigger=UseItem，可选 condition、additionalCondition、invokeTargetCondition、priority、targetCondition 等）。
2. 效果使用 `Effect.Damage`、`Effect.Shield`、`Effect.Heal`、**Effect.AddAttribute** / **Effect.ReduceAttribute**（己方加属性/敌方减属性，默认 Custom_0、SameAsSource）、`Effect.Haste`、`Effect.Repair`、`Effect.StartFlying`、`Effect.DestroyNextItemToRightOfCaster` 等；光环公式在 `AuraFormulaEvaluator` 中实现，新增公式时在 `Formula` 加常量、在 `AuraFormulaEvaluator.Evaluate` 加 case（依赖来源属性时传 `sourceIndex` 并用 `BattleAuraContext(side, sourceIndex)` 读值）。若物品有卡组可复写属性，设 `OverridableAttributes`（如 `{ nameof(ItemTemplate.StashParameter): [1,2,3,4] }`）。
3. 在对应 **RegisterAll** 中先设 `db.DefaultSize`（Small/Medium/Large），再按档位设 `db.DefaultMinTier`（如先 Bronze 注册所有铜，再 Silver 注册所有银），然后 `db.Register(XXX())`；Program/App 中需调用 `CommonLarge.RegisterAll`（若新增 CommonLarge 物品）。
4. 占位符由 `ItemDescHelper` 正则解析；时间用秒、内部 ToMilliseconds；`{AmmoCap}` 等自动支持。
