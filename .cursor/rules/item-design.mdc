---
description: 物品与效果设计经验：触发器、数值、效果委托、光环、Tooltip 与战斗内修改
alwaysApply: false
globs: "**/ItemDatabase/**","**/Core/Effect*.cs","**/Core/Ability*.cs","**/Core/ItemTemplate.cs","**/Core/Aura*.cs","**/Core/IEffectApplyContext.cs","**/Core/Condition.cs"
---

# 物品设计经验

## 物品模板（ItemTemplate）

- **基础**：`Name`、`Desc`、`Tags`（用 `Tag.Weapon`、`Tag.Friend`、`Tag.Tool`、`Tag.Apparel`、`Tag.Food`、`Tag.Tech`、`Tag.Property`、`Tag.Vehicle`、`Tag.Relic`、`Tag.Dragon`、`Tag.Drone`、`Tag.Toy` 等；**尺寸 Tag** `Tag.Small`、`Tag.Medium`、`Tag.Large` 由 **Register** 按 `DefaultSize` 自动添加，无需手写）。**MinTier、Size 不在物品工厂中设置**，由 **RegisterAll** 中 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`Register` 时写入模板（见 **project-conventions.mdc**、**implementation-notes.md** 物品定义简化一节）。
- **数值**：按等级用 `IntOrByTier`，支持单值或列表：`Damage = [5,10,15,20]`、`Burn = [6,9,12,15]`、`Poison = [1,2,3,4]`、`Custom_0 = [1,2,3,4]`。冷却用 `Cooldown = 5.0`（秒），无冷却则不设或为 0；**按 tier 不同冷却**时用 `CooldownMs = [9000, 8000, 7000]`（毫秒，与 MinTier 对应）。
- **时间一律用秒**：物品定义中表示时间时用秒以保证可读性（见 **project-conventions.mdc**）。充能用 `ChargeSeconds = 1.0`；冻结用 `FreezeSeconds`、`FreezeTargetCount`；减速用 `SlowSeconds`、`SlowTargetCount`；加速用 `HasteSeconds`、`HasteTargetCount`（目标数，默认 1），内部由模板 **SecondsOrByTier.ToMilliseconds()** 转为毫秒存储。多目标效果的目标选择：冻结/减速/充能/加速的**目标池为有冷却**物品，可选 `AbilityDefinition.TargetCondition`（默认冻结/减速为 DifferentSide，充能/加速为 SameSide）；**修复**的目标池为**已摧毁**物品，无时长，用 `RepairTargetCount`（默认 1），默认 TargetCondition 为 SameSide。
- **描述占位符**：`Desc` 中可用 `{Damage}`、`{Cooldown}`、`{Burn}`、`{Poison}`、`{Custom_0}` 等；支持前缀/后缀格式 `{+Custom_0%}`（大括号内非字母数字下划线的连续字符为前缀/后缀，与数值一并替换并参与加粗与 tier 着色）。`ItemDescHelper` 用正则动态解析，无需单独登记 key；`{Cooldown}` 映射为 `CooldownMs` 并按秒显示。
- **战斗内模板**：每场战斗 `BuildSide` 时每个槽位用卡组 template 克隆一份；`BattleItemState.Template` 即该克隆，可直接修改（如 `Template.Damage = Template.Damage.Add(delta)`），无需额外字段。
- **光环**：`Auras` 为 `List<AuraDefinition>`；每条含 `AttributeName`、`Condition`（委托式）、`FixedValueKey`、`PercentValueKey`、可选 `FixedValueFormula`。公式名用 **`Formula.SmallCountStash`**、**`Formula.SourceDamage`**（来源物品 Damage 含光环，用于如 Burn += 自身 Damage）等常量（`Core/Formula.cs`），不可手写字符串；公式实现集中在 `BattleSimulator/AuraFormulaEvaluator.cs`，不在 BattleAuraContext 内堆分支。光环条件除 `SameAsSource`、`AdjacentToSource`、`WithTag` 外，可用 **`AuraDefinition.SourceCondition = Condition.InFlight`**（提供光环的物品处于飞行状态，评估时 Item=Source=提供者）。ConditionContext 仅含 MySide、EnemySide、Item（被加成目标）、Source（光环提供者）。AttributeName/Key 用 `nameof(ItemTemplate.xxx)`。仅在战斗内读取属性且传入 `IAuraContext` 时生效；公式为 `(基础 + Σ固定) × (1 + Σ百分比/100)`，被摧毁物品不提供光环。战斗内读任意属性应走 **`BattleSide.GetItemInt(itemIndex, key, default)`**，统一带光环上下文。
- **可复写属性**：`OverridableAttributes` 为 `Dictionary<string, IntOrByTier>?`，表示卡组内可复写的属性及按 tier 的默认值；默认 null。拖入卡组或改 Tier 时用此初始化/更新槽位的 Overrides。如废品场长枪的 `StashParameter` 可复写，默认 `[1,2,3,4]`。
- **StashParameter**：`IntOrByTier`，用于如废品场长枪「小型物品数 + StashParameter」类公式，默认 0。
- **弹药**：`AmmoCap`（IntOrByTier，0 表示无弹药）；有弹药时使用后扣减 `AmmoRemaining`，日志可通过 `IBattleLogSink.OnCast(..., ammoRemainingAfter)` 显示剩余弹药。

## 触发器与能力

- **触发器命名**：常量与 UseItem/Freeze/Slow 保持一致风格，使用 `Trigger.Crit`、`Trigger.Destroy` 等（不带 On 前缀）；新增触发器时沿用同一命名习惯。ConditionContext 仅含 MySide/EnemySide/Item/Source?，由调用方传入不同 BattleItemState 表示「被评估物品」与「参考物品」。
- **使用物品**：冷却转好后施放，能力进入队列，按 250ms 间隔与优先级结算；`TriggerName = Trigger.UseItem`。**Priority 默认 Medium**（`AbilityDefinition.Priority` 默认值）；仅非 Medium 时显式写 `Priority = AbilityPriority.High` 等。
- **Ability 工厂**：单效果优先用 **Core/Ability.cs**：**Damage**、**Shield**、**Heal**、**Burn**、**Poison**、**Charge**、**Haste**、**Slow**、**Freeze**、**StartFlying**、**EndFlying**、**Destroy** 为**无参属性**，返回带默认值的 `AbilityDefinition`；**AddAttribute(attributeName, amountKey?)**、**ReduceAttribute(attributeName, amountKey?)** 为方法（需指定属性 key，如 `Key.Damage`）。定制时统一用 **Ability.xxx.Override(...)**，参数顺序与格式见 **.cursor/rules/ability-override-format.mdc**。数值 key 用 **Key.Damage**、**Key.Shield** 等（`Core/Key.cs`）。多效果或特殊 Condition 用 `new AbilityDefinition { ..., Apply = Effect.XXXApply, ... }`。
- **使用物品时「其他物品使用则触发」**：用 `Trigger.UseItem` + `Condition = DifferentFromSource & SameSide [ & 额外条件 ]`（如 `Condition.WithTag(Tag.Tool)`、`Condition.RightOfSource`（被使用物品在能力持有者右侧；评估时 Source=能力持有者、Item=被使用物品））。不再使用 `Trigger.UseOtherItem`。见 **battle-simulator-ability-queue.mdc**。
- **战斗开始**：第 0 帧触发一次；`TriggerName = Trigger.BattleStart`。模拟器通过统一 `InvokeTrigger` 入队，条件用 `Condition.Evaluate(ConditionContext)` 评估。
- **造成暴击时**：任意物品造成暴击时触发；`TriggerName = Trigger.Crit`。实现与 Freeze/Slow 一致：Condition 判定暴击施放者，默认 `Condition.SameSide`（表现为己方暴击时触发），可重写为实现对方暴击时触发。
- **施加摧毁时（Destroy）**：任意物品施加摧毁时触发；`TriggerName = Trigger.Destroy`。实现同 Slow：Condition 判定施加摧毁的物品，InvokeTargetCondition 判定被摧毁的物品；**须在将目标标记为 Destroyed 之前**调用 `InvokeTrigger(Trigger.Destroy, ...)`，context 传 `InvokeTargetItem`（被摧毁物品的 BattleItemState 引用）。被摧毁目标为大型或飞行时用 **`InvokeTargetCondition = Condition.WithTag(Tag.Large) | Condition.InFlight`**（尺寸 Tag 由注册时按 Size 自动添加；InFlight 表示被评估物品在飞行）。`EnsureTriggerCondition` 默认 `Condition.SameSide`；能力上常用 `Condition.SameAsSource` 表示「仅施加摧毁的物品自身」触发。
- **能力**：`AbilityDefinition` 含 `TriggerName`、`Priority`、`Condition`、`InvokeTargetCondition`、`TargetCondition`（效果选目标时目标需满足）、以及单条效果的 `Value`、`ValueKey`、`ApplyCritMultiplier`、`Apply`（`Action<IEffectApplyContext>?`）、`ResolveValue`。**一个能力一条 Apply**，无 Effects 列表（已与 Effect 合并，见 **implementation-notes.md** 能力与 Effect 合并重构）。

## 效果与数值（委托驱动）

- **效果应用**：`AbilityDefinition.Apply` 为 `Action<IEffectApplyContext>`；模拟器构建 `IEffectApplyContext` 后调用 `ability.Apply(ctx)`。预定义在 **Core/Effect.cs** 内为 *Apply 委托；只读效果在委托内用 `ctx.GetResolvedValue(nameof(ItemTemplate.XXX), ...)` 取值。**GetResolvedValue 必须带光环上下文**（如 `EffectApplyContextImpl` 用 `BattleAuraContext(Side, Item)` 传入）。多目标/定向效果的目标由能力 `TargetCondition` 经 `ctx.TargetCondition` 传入。
- **ValueKey**：指定了 ValueKey 的能力（如 AddAttribute、ReduceAttribute）由模拟器解析数值并填入 `ctx.Value`；模拟器仅在 `ability.ValueKey != null` 时解析。
- **暴击**：是否乘暴击由物品六字段与 `ability.ApplyCritMultiplier` 决定。日志「（暴击）」仅对可暴击效果显示。
- **AddAttribute / ReduceAttribute**：用 **Ability.AddAttribute** / **Ability.ReduceAttribute**；目标默认 AddAttribute 己方(SameSide)、ReduceAttribute 敌方(DifferentSide)。用 **additionalTargetCondition** 在默认上追加，如 `additionalTargetCondition: Condition.WithTag(Tag.Weapon)`（举重手套）、`Condition.WithTag(Tag.Shield)`（裂盾刀）；需完全自定义目标时用 **targetCondition**。**「某条件下此物品获得 X」**（如相邻物品触发减速时此物品获得剧毒）：**Condition** 描述谁触发（如 `condition: Condition.AdjacentToSource`），**TargetCondition** 必须为 **SameAsSource**（仅能力持有者享受效果），勿误用 additionalTargetCondition 导致加成给相邻物品。见 `docs/implementation-notes.md` 中「AddAttribute / ReduceAttribute 与统一属性增减」「触发条件与效果目标勿混淆」。
- **扩展效果**：在 `Core/Effect.cs` 新增静态 *Apply 委托或工厂方法，在物品定义中 `new AbilityDefinition { ..., Apply = Effect.XXXApply, ValueKey = ..., ApplyCritMultiplier = ... }` 或扩展 Ability 工厂。定向效果日志格式为「效果名 数值 →[目标]」；优先用 AddAttribute/ReduceAttribute。
- **飞行（In Flight）**：`BattleItemState.InFlight` 为运行时状态，战斗开始为 false。「开始飞行」用 **`Effect.StartFlying`**（设置 `InFlight = true` 并记日志「开始飞行」）；若施放者**已在飞行**则不重复结算、不记日志（幂等）。Tooltip 与日志中「飞行」着色与护盾同色（`EffectKeywordFormatting`）。
- **摧毁**：**`Effect.DestroyNextItemToRightOfCaster`** 摧毁己方施放者**右侧下一件未摧毁**物品（从 CasterItem.ItemIndex+1 起向右扫描）；由实现方先调用 Destroy 触发器、再标记 `Destroyed = true`。牵引光束等可在此基础上用 `Trigger.Destroy` + `Condition.SameAsSource` 造成伤害，用 `InvokeTargetCondition = Condition.ItemWithTag(Tag.Large) | Condition.InFlight` 判定被毁目标为大型或飞行时再造成伤害。日志中「摧毁」「修复」不显示数值（仅显示效果名与 extraSuffix，如「摧毁 →[物品名]」）；「摧毁」颜色 rgb(255,50,120)，见 **data-and-logging.mdc**。
- **物品类型 Tag**：判断「是否为护盾/伤害/灼烧物品」「是否可暴击」时使用模板的 `Tags`（`Tag.Shield`、`Tag.Damage` 等）。注册时由 `ItemDatabase.Register` 的 `EnsureTypeTags` 自动补充：① 模板任一档位下六类属性（Damage/Burn/Poison/Heal/Shield/Regen）> 0 则打对应 Tag；② 光环条件为 **SameAsSource** 且 **AttributeName** 为上述六类之一也打对应 Tag（如仅由光环提供伤害的物品）。无需在物品定义中手写。见 `docs/implementation-notes.md` 中「物品类型 Tag」。

## 战斗内修改与 IntOrByTier

- **IntOrByTier.Add(delta)**：每个 tier 的值都加 delta，返回新的 `IntOrByTier`；举重手套等「武器伤害 +N」用 **Ability.AddAttribute(Key.Damage).Override(additionalTargetCondition: ..., priority: ...)**，内部对己方满足 TargetCondition 的物品执行 `Template.Damage = Template.Damage.Add(value)`。
- **Damage 的 getter**：模板的 `Damage` 属性 get 返回完整按等级列表（`GetIntOrByTier(KeyDamage)`），以便支持 `.Add(delta)` 后 set 回；不要用仅返回单值的 getter 做此类修改。

## Tooltip 与 UI 表现

- **冷却行**：若物品无冷却（`CooldownMs` 为 0），Tooltip **不显示**「冷却时间」那一行（卡组内与物品池均按此判断）。
- **效果关键词**：`EffectKeywordFormatting` 中「伤害」「灼烧」「暴击」「暴击率」「暴击伤害」等仅**着色**、不加粗；加粗仅用于物品名称和占位符替换出的数值区段。颜色与战斗日志一致（如伤害/暴击系 rgb(0xf5,0x50,0x3d)，灼烧 rgb(255,159,69)，剧毒 rgb(14,190,79)）。
- **暴击伤害**：ItemTemplate 有 `CritDamagePercent`，默认 200（2 倍暴击）；暴击时倍率 = CritDamagePercent/100，作用于所有可暴击效果。光环可对自身 `SameAsSource` 增加 CritDamagePercent 百分比（如利爪 +100% → 400 即 4 倍）。
- **Desc 分行**：Desc 中可用分号（`;` 或 `；`）分段，Tooltip 会按段拆成多行显示。

## 灼烧衰减

- 每 500ms tick 时：衰减量 = 当前灼烧的 3%，用 `RatioUtil.PercentFloor(burn, 3)`（至少为 1）；`victim.Burn = Math.Max(0, victim.Burn - decay)`。这样灼烧 1 会衰减 1 变为 0。

## 新增公共物品流程

1. 按尺寸放入 `ItemDatabase/CommonSmall.cs`、`CommonMedium.cs` 或 **`CommonLarge.cs`**，写工厂方法，设置 Name、Desc、**Tags**、数值属性、**Abilities**（**不**设置 MinTier、Size）；若为纯光环或「基础 0 + 光环」可设 `Auras`，公式用 `FixedValueFormula = Formula.SmallCountStash` 等（见 `Core/Formula.cs`），不手写字符串。能力优先用 **Ability.Damage**、**Ability.Shield**、**Ability.Heal** 等无参属性，需定制时链式 **.Override(...)**（格式与参数顺序见 **ability-override-format.mdc**）；加/减属性用 **Ability.AddAttribute(Key.xxx)** / **Ability.ReduceAttribute(Key.xxx)**，再按需 .Override(...)。
2. 效果通过 **Ability.*** 工厂或 `new AbilityDefinition { Apply = Effect.XXXApply, ... }` 设置；加/减属性用 **Ability.AddAttribute** / **Ability.ReduceAttribute**（默认己方/敌方，用 additionalTargetCondition 收窄）；无工厂时用 `Effect.HasteApply`、`Effect.RepairApply`、`Effect.StartFlyingApply`、`Effect.DestroyNextItemToRightOfCasterApply` 等；光环公式在 `AuraFormulaEvaluator` 中实现，新增公式时在 `Formula` 加常量、在 `AuraFormulaEvaluator.Evaluate` 加 case（依赖来源属性时传 `sourceIndex` 并用 `BattleAuraContext(side, sourceIndex)` 读值）。若物品有卡组可复写属性，设 `OverridableAttributes`（如 `{ nameof(ItemTemplate.StashParameter): [1,2,3,4] }`）。
3. 在对应 **RegisterAll** 中先设 `db.DefaultSize`（Small/Medium/Large），再按档位设 `db.DefaultMinTier`（如先 Bronze 注册所有铜，再 Silver 注册所有银），然后 `db.Register(XXX())`；Program/App 中需调用 `CommonLarge.RegisterAll`（若新增 CommonLarge 物品）。
4. 占位符由 `ItemDescHelper` 正则解析；时间用秒、内部 ToMilliseconds；`{AmmoCap}` 等自动支持。
