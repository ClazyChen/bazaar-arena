---
description: CLI 与物品测试流程；完成任务后用 CLI 跑测试，失败时在文档标注未通过
alwaysApply: true
---

# CLI 与物品测试

- **测试工具**：以 **CLI**（`src/BazaarArena.Cli`）为运行对战的测试工具；自动化脚本为 **`scripts/run_item_tests.py`**（在仓库根目录执行 `python scripts/run_item_tests.py`）。详见 **docs/cli-and-testing.md**。
- **测试方法**：用 CLI 指定卡组集 JSON 与两个卡组 ID，跑一场对战并将详细日志写入文件；对日志内容做**字符串包含**断言（如「灼烧结算」「冻结」「伤害 6」）。不通过放宽断言来通过测试。
- **测试流程**：完成任务或修改物品/模拟器后，应运行 `python scripts/run_item_tests.py` 做回归；若某用例失败，在 **docs/test-cases-small-bronze.md**（或对应测试用例文档）与 **docs/items-list.md** 中标注该物品/用例「**未通过测试**」，并写明失败原因与日志路径（如 `Logs/item_tests/xxx.log`），以待后续检查与修复。
- **新增用例**：新增物品或新测试套件时，在卡组集 JSON 中增加卡组、在 `run_item_tests.py` 中增加 `log_contains` 等断言、在测试用例文档与物品列表中补充说明与测试状态列。
