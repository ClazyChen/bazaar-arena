---
description: CLI 与物品测试流程；完成任务后用 CLI 跑测试，失败时在文档标注未通过
alwaysApply: true
---

# CLI 与物品测试

- **测试工具**：以 **CLI**（`src/BazaarArena.Cli`）为运行对战的测试工具。物品测试按档位分多套脚本（仓库根目录执行）：**小型铜** `python scripts/item_tests/run_item_tests_small_bronze.py`、**小型银** `python scripts/item_tests/run_item_tests_small_silver.py`、**中型铜** `python scripts/item_tests/run_item_tests_medium_bronze.py`、**大型铜** `python scripts/item_tests/run_item_tests_large_bronze.py`；**海盗小型铜** `python scripts/item_tests/run_item_tests_vanessa_small_bronze.py`（卡组集同 `Data/Decks/item_tests/test_small_bronze.json`，ID 前缀 vanessa_sb_*）；**海盗中型铜** `python scripts/item_tests/run_item_tests_vanessa_medium_bronze.py`（卡组集同 `Data/Decks/item_tests/test_medium_bronze.json`，ID 前缀 vanessa_mb_*）。卡组集路径见 **docs/cli-and-testing.md**。
- **测试方法**：用 CLI 指定卡组集 JSON 与两个卡组 ID，跑一场对战并将详细日志写入文件；对日志内容做**字符串包含**断言（`log_contains`）；需要「至少出现 N 次」时使用可选 **`log_min_count`**（如牵引光束第二次伤害）。不通过放宽断言来通过测试。
- **批量执行**：物品测试脚本应优先使用 CLI 的 **`--batch`** 模式，在一次进程内完成多个测试点，以降低「dotnet 启动」开销；脚本负责构造批量配置 JSON（包含多组 deck1/deck2 与各自日志路径）。
- **测试流程**：完成任务或修改物品/模拟器后，根据物品档位运行对应脚本做回归；若某用例失败，在对应测试用例文档（**docs/test-cases-small-bronze.md**、**test-cases-small-silver.md**、**test-cases-medium-bronze.md**、**test-cases-large-bronze.md**、**test-cases-vanessa-small-bronze.md**、**test-cases-vanessa-medium-bronze.md**）与 **docs/items-list.md** 中标注该物品/用例「**未通过测试**」，并写明失败原因与日志路径（如 `Logs/item_tests/xxx.log`）；**修复通过后须同步更新为「通过」并移除旧标注**。提交前建议搜索「未通过测试」检查是否仍有与当前结果不符的旧标注。
- **新增用例**：新增物品或新测试套件时，在对应卡组集 JSON 中增加卡组、在相应脚本中增加 `log_contains`（及可选 `log_min_count`）、在 **docs/test-cases-*.md** 与 **docs/items-list.md** 中补充说明与测试状态列；保持脚本与 CLI 批量配置同步更新。
- **经验**：**纯光环物品**（如外骨骼）无施放/效果日志，断言应验证受光环影响的结果（如相邻武器「伤害 10」= 模板基础伤害 + 光环，随獠牙等词条变更需同步更新断言）而非物品名。需要「修复已摧毁」「被毁为飞行时再伤害」等状态时，可在卡组内用牵引光束、宇宙护符+友好玩偶等**创造触发条件**再断言效果；卡组可跨尺寸/档位混用。
