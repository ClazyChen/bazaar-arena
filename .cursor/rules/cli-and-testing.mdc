---
description: CLI 与物品测试流程；完成任务后用 CLI 跑测试，失败时在文档标注未通过
alwaysApply: true
---

# CLI 与物品测试

- **测试工具**：以 **CLI**（`src/BazaarArena.Cli`）为运行对战的测试工具；小型铜物品自动化脚本为 **`scripts/run_item_tests_small_bronze.py`**（在仓库根目录执行 `python scripts/run_item_tests_small_bronze.py`），小型银物品脚本为 **`scripts/run_item_tests_small_silver.py`**。详见 **docs/cli-and-testing.md**。
- **测试方法**：用 CLI 指定卡组集 JSON 与两个卡组 ID，跑一场对战并将详细日志写入文件；对日志内容做**字符串包含**断言（如「灼烧结算」「冻结」「伤害 6」）。不通过放宽断言来通过测试。
- **批量执行**：物品测试脚本应优先使用 CLI 的 **`--batch`** 模式，在一次进程内完成多个测试点，以降低「dotnet 启动」开销；脚本负责构造批量配置 JSON（包含多组 deck1/deck2 与各自日志路径）。
- **测试流程**：完成任务或修改物品/模拟器后，应根据物品档位运行 `python scripts/run_item_tests_small_bronze.py` 或 `python scripts/run_item_tests_small_silver.py` 做回归；若某用例失败，在对应测试用例文档（如 **docs/test-cases-small-bronze.md**、**docs/test-cases-small-silver.md**）与 **docs/items-list.md** 中标注该物品/用例「**未通过测试**」，并写明失败原因与日志路径（如 `Logs/item_tests/xxx.log`），以待后续检查与修复。
- **新增用例**：新增物品或新测试套件时，在对应卡组集 JSON 中增加卡组、在相应脚本（如 `run_item_tests_small_bronze.py`、`run_item_tests_small_silver.py`）中增加 `log_contains` 等断言、在测试用例文档与物品列表中补充说明与测试状态列；保持脚本与 CLI 批量配置同步更新。
