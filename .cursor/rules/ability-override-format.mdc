---
description: Ability.xxx.Override(...) 的代码风格与参数顺序
alwaysApply: false
globs: "**/ItemDatabase/**","**/Core/Ability*.cs"
---

# Override / Also 格式要求

在物品定义或能力相关代码中书写 **Ability.xxx.Override(...)**，以及在现有能力基础上追加触发条件的 **ability.Also(...)** 时，须遵守以下格式与参数顺序。

## 格式

- **Override(** / **Also(** 之后**换行并缩进**，每个指定参数**单独一行**。
- 最后一个参数后**不**加逗号；然后**换行**、回到与 `Ability.xxx` 或能力变量对齐的缩进、写 `)**。

## 简化原则

当能力的**默认 TargetCondition** 已是 `Condition.SameSide`（如 AddAttribute、Charge、Haste）或 `Condition.DifferentSide`（如 ReduceAttribute、Slow、Freeze）时，若只需在默认上**追加**条件，应使用 **additionalTargetCondition** 而非重写 **targetCondition**，以保持简洁并与默认语义一致。

- 示例：目标为「己方且飞行」→ 写 `additionalTargetCondition: Condition.InFlight`，勿写 `targetCondition: Condition.SameSide & Condition.InFlight`。
- 示例：目标为「己方武器」→ 写 `additionalTargetCondition: Condition.WithTag(Tag.Weapon)`。

## 参数顺序

按以下顺序书写（未指定的参数省略即可）：

1. **trigger**
2. **condition** / **additionalCondition**
3. **invokeTargetCondition**
4. **sourceCondition**
5. **targetCondition** / **additionalTargetCondition**
6. **priority**

其余可选参数（如 valueKey、value、applyCritMultiplier、apply）如确需覆盖，放在 priority 之后即可。  
对于 **ability.Also(...)**，通常只传前四类参数（trigger / condition / invokeTargetCondition / sourceCondition），不应在 Also 中修改 priority / valueKey / value / targetCondition 等共享属性。

## 示例

「使用工具时为此物品充能」等「当其他某类物品被使用」须用 **condition: Condition.SameSide & Condition.WithTag(Tag.xxx)**，不能用 additionalCondition。

```csharp
Ability.AddAttribute(Key.Damage).Override(
    trigger: Trigger.Freeze,
    additionalTargetCondition: Condition.WithTag(Tag.Weapon),
    priority: AbilityPriority.Low
),
Ability.Charge.Override(
    condition: Condition.SameSide & Condition.WithTag(Tag.Tool),
    targetCondition: Condition.SameAsSource
),
Ability.Damage.Override(
    trigger: Trigger.Destroy,
    condition: Condition.SameAsSource,
    invokeTargetCondition: Condition.WithTag(Tag.Large) | Condition.InFlight
),
```

单参数时也建议换行书写，保持风格一致：

```csharp
Ability.Burn.Override(
    trigger: Trigger.BattleStart
),

// 多触发示例：同一条能力在「战斗开始」与「使用物品」时都能触发，但共享数值与 250ms 触发间隔
Ability.Burn.Override(
    trigger: Trigger.BattleStart,
    condition: Condition.SameSide
).Also(
    trigger: Trigger.UseItem,
    condition: Condition.SameAsSource
),
```
