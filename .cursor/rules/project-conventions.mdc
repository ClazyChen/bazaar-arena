---
description: Bazaar Arena 项目通用约定（C#、Windows、中文）
alwaysApply: true
---

# 项目约定

- **项目名称**：Bazaar Arena（游戏 The Bazaar 的组队模拟器）。代码与命名空间使用 `BazaarArena`。
- **平台**：面向 Windows；采用 C# 编写。
- **语言**：用户界面文案、代码注释均使用中文。
- **模块与目录**：与设计文档一致，主项目下分为：
  - `DeckManager/` — 卡组管理器
  - `ItemDatabase/` — 物品数据库
  - `BattleSimulator/` — 对战模拟器

命名空间与文件夹对应，例如 `BazaarArena.DeckManager`、`BazaarArena.ItemDatabase`、`BazaarArena.BattleSimulator`。

- **物品定义中的时间**：表示时间时，在物品定义代码中一律使用**秒**作为单位以保证可读性（如 `FreezeSeconds = new[] { 3.0, 4.0, 5.0, 6.0 }`、`Cooldown = 5.0`、`ChargeSeconds = 1.0`）；内部存储可为毫秒，由模板属性做转换（`SecondsOrByTier.ToMilliseconds()`）。

- **物品注册与尺寸/档位**：物品模板中**不**在工厂方法里写 `MinTier`、`Size`；在 **RegisterAll** 中通过 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`ItemDatabase.Register` 会将当前默认值写入模板并调用 **EnsureTypeTags**（按 Ability.Apply 打类型/机制 Tag，按 UseItem+ApplyCritMultiplier 与六类 Tag 打 Tag.Crit，按 CooldownMs 打 Tag.Cooldown，SameAsSource 光环补六类 Tag）后再存入。能力优先使用 **Ability.***：除 **AddAttribute(attributeKey, amountKey?)**、**ReduceAttribute(attributeKey, amountKey?)** 为方法（需指定属性 key）外，其余为**无参属性**（如 `Ability.Damage`、`Ability.Haste`），每次访问返回新的带默认值的 `AbilityDefinition`；定制时统一用 **Ability.xxx.Override(...)** 链式调用，仅 Override 传入的参数会覆盖默认。数值/属性 key 在能力与物品定义侧使用 **Key.Damage**、**Key.Shield**、**Key.Heal**、**Key.Burn**、**Key.Poison**、**Key.Custom_0**（`Core/Key.cs`），替代 `nameof(ItemTemplate.xxx)`。**一个能力一条 Apply**（能力与 Effect 已合并，无 Effects 列表）；默认 trigger 为 UseItem，Condition/TargetCondition 的合并在 **AbilityDefinition.Override** 内完成（将当前定义作为默认与传入的 condition、additionalCondition、targetCondition、additionalTargetCondition 合并）。**UseItem / UseOtherItem 语义**：UseItem=使用自己；UseOtherItem=其他物品被使用（默认 condition 为 SameSide & DifferentFromCaster，可 override 为 DifferentSide 等）。**AbilityDefinition.Priority** 默认 Medium。**AddAttribute/ReduceAttribute** 时：Condition = 何时触发（引起触发的物品），TargetCondition = 效果施加给谁；文案为「此物品获得 X」时须用 **targetCondition: Condition.SameAsSource**，勿把触发范围（如相邻）误写在 additionalTargetCondition 上。**UseOtherItem 下「当其他某类物品被使用」**（如使用工具时）优先写 `condition: Condition.SameSide & Condition.DifferentFromCaster & Condition.WithTag(Tag.xxx)`，不能用 additionalCondition。**暴击**：按物品按帧统一判定（每物品每帧只掷一次，结果存于物品 CritTimeMs/IsCritThisUse/CritDamagePercentThisUse）；Crit 触发生在 ExecuteOneEffect 之前、本帧首次判定为暴击时一次。**add/reduce 暴击率**：效果层对 CritRatePercent 使用 **Condition.CanCrit**（由 `ItemState.CanCrit` 判定：具备六类可暴击 DerivedTag 且至少一条 UseItem+ApplyCritMultiplier 能力），仅可暴击物品才可被加/减暴击率。**Override 的代码风格与参数顺序**见 **.cursor/rules/ability-override-format.mdc**；**condition 与 additionalCondition 二选一**，不可同时指定，见该规则「简化原则」。定义物品时**能简化的要简化**：在默认目标（SameSide/DifferentSide）上仅追加条件时用 **additionalTargetCondition**，不重写 targetCondition。充能、加速、减速、冻结、修复、摧毁等目标数量在效果层默认 1（GetResolvedValue(..., defaultValue: 1)），物品定义中为 1 时可省略 **ChargeTargetCount**、**HasteTargetCount**、**SlowTargetCount**、**FreezeTargetCount**、**RepairTargetCount**、**DestroyTargetCount**。**IntOrByTier 单值赋值**（如 `ChargeTargetCount = 10`）在模板中直接写即可，`int → IntOrByTier` 隐式转换已保证写入非空列表，无需在工厂方法内再写 SetInt 补丁；详见 `docs/implementation-notes.md`「IntOrByTier 单值隐式转换」。摧毁用 **Ability.Destroy**（目标仅要求未摧毁、不要求有冷却）；「右侧第一个未摧毁」用 **additionalTargetCondition: Condition.FirstNonDestroyedRightOfSource**。光环用 **AuraDefinition.Value**（Formula?）与 **Percent**（bool）；数值用 **Formula.Source(key)**、**Formula.Opp(key)**、**Formula.Count(condition)** 等组合。**AuraDefinition.Condition** 默认为 SameAsSource，作用自身时**不写** Condition。`AuraDefinition` 已无 `SourceCondition` 与 `GrantedTags`：若「仅当提供者满足某条件时生效」，请把条件并入 `Condition`（常见写法 `Condition.SameAsSource & ...`）；若需授予标签，使用 `Attribute = Key.Tags`、`Value = Formula.Constant(Tag.xxx | Tag.yyy)`、`Percent = false`，并按标签掩码语义处理。**OverridableAttributes** 只写一次默认值即可，Register 会同步到模板，不在模板上重复定义同名属性。需复杂计算时用 **Formula.*** 与 **+/-/***、**一元负号**、**按条件取反 `~Formula`**、**int * Formula** 组合。目标/触发过滤中的“非”语义统一写 `~Condition.xxx`，不再新增 `Condition.NotXxx`。百分比用 **RatioUtil.PercentFloor(Formula, percent)** 或 **RatioUtil.PercentFloor(Formula, Formula)**（percent 来自字段/公式时）。护盾/伤害等「等量于 X」的**动态数值**用**光环**为该属性提供 Formula（如 Value = Formula.Opp(Key.Burn)），配合原有 **Ability.Shield**/**Ability.Damage**，不新增专用 Ability。战斗内物品变量存于 **ItemState.Attributes**（**Key.SideIndex** 起与模板下标对齐，运行时槽在 **Key.InFlight** 之后）；阵营数值存于 **BattleSide.Attributes**（**Key.MaxHp**/**Key.Hp** 与 **Key.Damage**/**Key.Heal** 同下标；Shield/Burn/Poison/Regen/**Key.Gold** 复用同名 Key）。**BattleSide.GetInt(string)** 仅用于按键名（如卡组 JSON）解析；热路径用 **Key.* 下标** 与 **GetItemInt(itemIndex, Key.xxx)**。

- **Ability 多触发与 Ability.Also**：当同一条效果语义需要在多个触发器/条件下生效、且数值、目标与优先级完全一致并需要共享 250ms 触发间隔时，优先使用一条能力 + 多次 **ability.Also(...)**（如 `Ability.Damage.Override(...).Also(trigger: Trigger.BattleStart, condition: ...)`），而不是复制多条几乎相同的能力定义；`Also` 的参数顺序与缩进风格与 `Override` 保持一致（先 trigger，再 condition/additionalCondition，再 invokeTargetCondition/sourceCondition），不修改 Priority/TargetCondition/ValueKey 等共享属性。

- **Tag / DerivedTag 分离**：普通标签（显式标签 + `Small/Medium/Large`）仅使用 **Tag** 并写入 `Key.Tags`；自动推导标签（如 `Damage`、`Shield`、`Ammo`、`Burn`、`Poison`、`Heal`、`Regen`、`Crit`、`Cooldown`、`Charge`、`Freeze`、`Slow`、`Haste`、`Reload`）仅使用 **DerivedTag** 并写入 `Key.DerivedTags`。`DerivedTag` 从 `1<<0` 独立编号；标签判断时必须按语义拆分：**`Condition.WithTag` 仅判断 `Key.Tags`**、**`Condition.WithDerivedTag` 仅判断 `Key.DerivedTags`**（两者均通过 `GetItemInt` 感知光环影响），不得把两类标签混为同一常量域扩展。

- **Apply 统一执行入口**：能力定义统一通过 **Apply.\*** 执行效果（**`Ability.*`** 默认绑定 **`Apply`**）；**`AbilityDefinition.Apply`** 为 **`Action<BattleContext>?`**，由模拟器统一能力执行入口写入最小上下文字段（`BattleState/Item/Caster/Source/InvokeTarget`）后调用。新增能力类型时在 **`Core/Apply.cs`** 增加委托或自定义 **`Action<BattleContext>`**，再接入 **`Ability`** 工厂。

- **模板复用与实例化**：`ItemDatabase` 注册后模板按引用复用，不再提供/依赖 `ItemTemplate` 战斗克隆；卡组实例化统一 `new ItemState(template, tier)`（含扁平化路径）。战斗内可变数值（冷却、伤害增减、暴击率、弹药等）写入 `ItemState.Attributes`，`BuildSide` 仅把 slot overrides 应用到 `ItemState`（且仅限 `OverridableAttributes` 声明 key）。

- **物品多版本命名**：同一物品可有多个版本注册；**无后缀**表示最新版本，**`[物品名]_Sx`** 表示第 x 赛季版本（如 `獠牙_S10`）。历史版本同样用**基名**（去掉 `_Sx`）定位显示图片（`pictures/png/<基名>.png`）。GUI 默认物品池只显示最新版本（`GetLatestOnlyNames()`）；可选物品区与卡组内**右键**可循环切换版本（最新→较高赛季→较低赛季→最新），左键可将当前显示的版本拖入卡组；左键点击其他位置且未拖拽入卡组时，池中已切换到历史版本的项会重置为显示最新版本。**卡组内**版本切换仅在与当前槽位 **MinTier** 一致的版本间循环（避免铜槽切到银版本）。详见 `docs/implementation-notes.md`「多版本物品与 GUI 版本切换」。
- **GUI 卡组集**：启动时优先打开 **上次打开的卡组集**（路径存于 Data/last-collection.txt）；无记录、文件不存在或打开失败时使用 default.json；打开/新建另存为/保存另存为成功时更新记录。详见 implementation-notes「GUI 启动时上次打开的卡组集」。

- **与游戏对齐的物品与测试物品**：`ItemDatabase/CommonSmall.cs`、`CommonMedium.cs`、`CommonLarge.cs` 中已有物品用于对齐当前版本的 The Bazaar，不得为调试/实验随意修改数值、文案或能力行为；如需验证新机制（如 Ability 多触发），应在 `ItemDatabase` 下新建独立文件定义测试物品，并在对应 `RegisterAll` 中**临时**注册，测试通过后移除这些注册语句，避免测试物品进入正式卡组。

- **英雄/关卡专属物品**：海盗等英雄或关卡专属物品**不**放在 CommonSmall/CommonMedium/CommonLarge，应放在 **ItemDatabase/&lt;英雄名&gt;/&lt;尺寸&gt/** 下（如 `Vanessa/small`）。一物一文件，类名与英文名对应（如 `ConcealedDagger.cs`）；工厂方法 **Template()** 表示当前/最新版本，**Template_Sx()** 表示第 x 赛季版本。在对应英雄的 **RegisterAll** 中设置 **db.DefaultHero**（如 `Hero.Vanessa`）、**db.DefaultSize**、**db.DefaultMinTier** 后按档位分批 **db.Register(...)**。详见 `docs/implementation-notes.md`「英雄/关卡专属物品与目录结构」。

- **对战模拟器能力队列**：修改 `BattleSimulator` 中能力队列、施放队列或触发器逻辑时，必须遵守 **.cursor/rules/battle-simulator-ability-queue.mdc**（仅步骤 9 做 next→current、cast 触发的能力入 next、步骤 8 只处理 current、新能力按优先级入 current/next、PendingCount 先 current 再 next）。

- **效果应用上下文**：使用 **`BattleContext`**（由模拟器统一能力执行入口按最小字段直接赋值，不再使用 `Rebind`）；施放者与默认 **`Item`** 一致（**`Caster`/`CasterItem`**）；**`InvokeTarget`** 表示触发器指向的单一目标（见 implementation-notes「InvokeTarget 与 SameAsInvokeTarget」）。冻结/减速/摧毁等「施加时触发」通过 **EffectAppliedTriggerQueue** 上报，**`BattleContext`** 只追加三元组，模拟器在 **Apply** 后统一 **`InvokeTrigger`** 并处理 **Destroyed**。详见 **「BattleContext 与施加触发器队列」**。**属性增减**、**冻结/减速/加速/充能**、**弹药触发器**、**摧毁**等语义不变，实现位于 **`Core/BattleContext.EffectApply.cs`**；日志默认用 **`AttributeLogNames`**（**`Core/AttributeLogNames.cs`**）。
- **硬切迁移约束（2026-03）**：`AbilityDefinition.Override` 已无 `invokeTargetCondition` 参数，物品定义中禁止继续书写该参数名；触发器指向目标相关语义分别用 `condition`/`additionalCondition`（触发条件）与 `additionalTargetCondition: Condition.SameAsInvokeTarget`（效果目标）表达。`OverridableAttributes` 统一为 `Dictionary<int, IntOrByTier>`；GUI/Deck 的 `Overrides` 仍为 `Dictionary<string,int>` 时必须通过 `Key.GetName/TryGetKey` 做映射桥接，禁止回退到 string-key 主数据模型。

- **从表格添加物品**：列顺序为 **中文名、英文名、所属、版本、minTier、Size、CD、TAG、效果**；**英文名为第 2 列**（后续不再单独给出，从表格读取）；**版本为第 4 列**，数字对应 _Sx 或无后缀。**表格第一行**为最新版、名称**无后缀**（Template()、Name 不带 _Sx）；效果**未标注** (I)/(Hst)/(H)/(L)/(Lst) 则为 **Medium**，不猜测、不擅自写 priority。**▶** 与「**提高**」「造成」等主动动词 = **使用物品时**触发的 Ability，勿误写为被动光环；「购买时获得 X」等**局外成长**不实现。详见 **.cursor/rules/item-table-convention.mdc** 与 `docs/implementation-notes.md`「从表格添加物品的约定」。
- **InvokeTrigger 次序**：调用触发器时按 **`SessionTables.AbilitiesByTrigger[triggerName]`** 直接遍历匹配能力，不再保证“先 causeItem 再同侧再异侧”。UseItem / UseOtherItem 时 context 仍传入 **InvokeTargetItem = 被使用物品**，便于「对被使用的那件物品施加效果」用 **SameAsInvokeTarget**。
- **源文件长度与拆分**：除 `ItemDatabase/CommonSmall.cs`、`CommonMedium.cs` 等以数据定义为主的文件外，单文件不宜过长。可将嵌套类、静态辅助拆到同命名空间下的独立源文件（例如 BattleSimulator 下的 **`BattleSideDamage.cs`**、**`TriggerInvokeContext.cs`**；Core 下的 **`BattleContext.EffectApply.cs`**）。详见 **`docs/implementation-notes.md`**「BattleSimulator 文件拆分与代码结构」。

- **CLI 与测试**：完成任务后用 **CLI** 与物品测试脚本做验证；脚本位于 **`scripts/item_tests/`**，卡组集 JSON 位于 **`Data/Decks/item_tests/`**（如 `test_small_bronze.json`）。常用示例：小型铜 `run_item_tests_small_bronze.py`、中型银 `run_item_tests_medium_silver.py`、中型金/钻 `run_item_tests_medium_gold_diamond.py`、大型银/金/钻 `run_item_tests_large_silver_gold_diamond.py`、海盗小型/中型铜 `run_item_tests_vanessa_*` 等。**完整命令与档位列表**见 **docs/cli-and-testing.md** 与 **.cursor/rules/cli-and-testing.mdc**。测试未通过时在文档中标注「未通过测试」、不放松断言。

- **Git 提交信息**：提交信息须符合 **.cursor/rules/git-commit-format.mdc**（`<type>(<scope>): <subject>`，中文主题，如 `feat(gui): 卡组 MinTier 显示与拖拽`）。

- **ValueKey 与运行时读值（2026-03）**：能力执行路径统一由 **`Apply`** 通过 `ctx.GetItemInt(ctx.Caster, ability.ValueKey!.Value)` 读取数值；`AbilityDefinition.Value` 与 `Override(value: ...)` 已移除，不再允许新增。常量效果请改为模板属性（通常 `Custom_x`）并在能力上设置 `valueKey`。`BattleContext.GetResolvedValue` 已移除；默认值应在 `ItemDatabase.Register` 阶段补齐（如 `ModifyAttributeTargetCount=20`），运行时仅保留最小兼容兜底（如目标数为 0 时按 1）。

- **PoisonSelf 语义**：`Ability.PoisonSelf` 为独立能力语义（对自身施加剧毒），不得与 `Ability.Poison` 互相替代；区别在施加目标（自身 vs 对方），触发上报均为 `Trigger.Poison`。
