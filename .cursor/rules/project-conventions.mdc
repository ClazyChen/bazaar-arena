---
description: Bazaar Arena 项目通用约定（C#、Windows、中文）
alwaysApply: true
---

# 项目约定

- **项目名称**：Bazaar Arena（游戏 The Bazaar 的组队模拟器）。代码与命名空间使用 `BazaarArena`。
- **平台**：面向 Windows；采用 C# 编写。
- **语言**：用户界面文案、代码注释均使用中文。
- **模块与目录**：与设计文档一致，主项目下分为：
  - `DeckManager/` — 卡组管理器
  - `ItemDatabase/` — 物品数据库
  - `BattleSimulator/` — 对战模拟器

命名空间与文件夹对应，例如 `BazaarArena.DeckManager`、`BazaarArena.ItemDatabase`、`BazaarArena.BattleSimulator`。

- **物品定义中的时间**：表示时间时，在物品定义代码中一律使用**秒**作为单位以保证可读性（如 `FreezeSeconds = new[] { 3.0, 4.0, 5.0, 6.0 }`、`Cooldown = 5.0`、`ChargeSeconds = 1.0`）；内部存储可为毫秒，由模板属性做转换（`SecondsOrByTier.ToMilliseconds()`）。

- **物品注册与尺寸/档位**：物品模板中**不**在工厂方法里写 `MinTier`、`Size`；在 **RegisterAll** 中通过 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`ItemDatabase.Register` 会将当前默认值写入模板并调用 **EnsureTypeTags**（按属性与 SameAsSource 光环补充护盾/伤害等类型 Tag）后再存入。能力优先使用 **Ability.***：除 **AddAttribute(attributeKey, amountKey?)**、**ReduceAttribute(attributeKey, amountKey?)** 为方法（需指定属性 key）外，其余为**无参属性**（如 `Ability.Damage`、`Ability.Haste`），每次访问返回新的带默认值的 `AbilityDefinition`；定制时统一用 **Ability.xxx.Override(...)** 链式调用，仅 Override 传入的参数会覆盖默认。数值/属性 key 在能力与物品定义侧使用 **Key.Damage**、**Key.Shield**、**Key.Heal**、**Key.Burn**、**Key.Poison**、**Key.Custom_0**（`Core/Key.cs`），替代 `nameof(ItemTemplate.xxx)`。**一个能力一条 Apply**（能力与 Effect 已合并，无 Effects 列表）；默认 trigger 为 UseItem，Condition/TargetCondition 的合并在 **AbilityDefinition.Override** 内完成（将当前定义作为默认与传入的 condition、additionalCondition、targetCondition、additionalTargetCondition 合并）。**AbilityDefinition.Priority** 默认 Medium。**AddAttribute/ReduceAttribute** 时：Condition = 何时触发（引起触发的物品），TargetCondition = 效果施加给谁；文案为「此物品获得 X」时须用 **targetCondition: Condition.SameAsSource**，勿把触发范围（如相邻）误写在 additionalTargetCondition 上。**UseItem 下「当其他某类物品被使用」**（如使用工具时）须用 **condition: Condition.SameSide & Condition.WithTag(Tag.xxx)**，不能用 additionalCondition。**UseSelf**：Trigger 为 UseItem 且未在 Override 中提供 condition 时为 true（仅用 additionalCondition 不变）；仅 UseSelf 能力可参与暴击判定。**Override 仅改 trigger** 时若非 UseItem 须置 UseSelf = false（else if 分支内）。**暴击**：按物品按帧统一判定（每物品每帧只掷一次，结果存于物品 CritTimeMs/IsCritThisUse/CritDamagePercentThisUse）；Crit 触发生在 ExecuteOneEffect 之前、本帧首次判定为暴击时一次。**add/reduce 暴击率**：效果层对 CritRatePercent 使用 **Condition.CanCrit**（HasAnyCrittableTag 且至少一条 UseItem+UseSelf+ApplyCritMultiplier 能力），仅可暴击物品才可被加/减暴击率。**Override 的代码风格与参数顺序**见 **.cursor/rules/ability-override-format.mdc**。定义物品时**能简化的要简化**：在默认目标（SameSide/DifferentSide）上仅追加条件时用 **additionalTargetCondition**，不重写 targetCondition。充能、加速、减速、冻结、修复、摧毁等目标数量在效果层默认 1（GetResolvedValue(..., defaultValue: 1)），物品定义中为 1 时可省略 **ChargeTargetCount**、**HasteTargetCount**、**SlowTargetCount**、**FreezeTargetCount**、**RepairTargetCount**、**DestroyTargetCount**。**IntOrByTier 单值赋值**（如 `ChargeTargetCount = 10`）在模板中直接写即可，`int → IntOrByTier` 隐式转换已保证写入非空列表，无需在工厂方法内再写 SetInt 补丁；详见 `docs/implementation-notes.md`「IntOrByTier 单值隐式转换」。摧毁用 **Ability.Destroy**（目标仅要求未摧毁、不要求有冷却）；「右侧第一个未摧毁」用 **additionalTargetCondition: Condition.FirstNonDestroyedRightOfSource**。光环用 **AuraDefinition.Value**（Formula?）与 **Percent**（bool）；数值用 **Formula.Source(key)**、**Formula.Opp(key)**、**Formula.Count(condition)** 等组合。**AuraDefinition.Condition** 默认为 SameAsSource，作用自身时**不写** Condition。**OverridableAttributes** 只写一次默认值即可，Register 会同步到模板，不在模板上重复定义同名属性。若「仅当提供者满足某条件时生效」用 **SourceCondition**（如 **Condition.OnlyCompanion**、**Condition.InFlight**）。需复杂计算时用 **Formula.*** 与 **+/-/***、**一元负号**、**int * Formula** 组合；百分比用 **RatioUtil.PercentFloor(Formula, percent)** 或 **RatioUtil.PercentFloor(Formula, Formula)**（percent 来自字段/公式时）。护盾/伤害等「等量于 X」的**动态数值**用**光环**为该属性提供 Formula（如 Value = Formula.Opp(BattleSide.KeyBurn)），配合原有 **Ability.Shield**/**Ability.Damage**，不新增专用 Ability。战斗内产生的变量（SideIndex、ItemIndex、Tier、CooldownElapsedMs、InFlight、Destroyed 等）存于 **ItemTemplate** 字典；阵营数值（MaxHp、Hp、Shield、Burn、Poison、Regen）存于 **BattleSide** 字典，均可通过 **GetInt(key)**/名字一致解析。

- **Ability 多触发与 Ability.Also**：当同一条效果语义需要在多个触发器/条件下生效、且数值、目标与优先级完全一致并需要共享 250ms 触发间隔时，优先使用一条能力 + 多次 **ability.Also(...)**（如 `Ability.Damage.Override(...).Also(trigger: Trigger.BattleStart, condition: ...)`），而不是复制多条几乎相同的能力定义；`Also` 的参数顺序与缩进风格与 `Override` 保持一致（先 trigger，再 condition/additionalCondition，再 invokeTargetCondition/sourceCondition），不修改 Priority/TargetCondition/ValueKey 等共享属性。

- **物品多版本命名**：同一物品可有多个版本注册；**无后缀**表示最新版本，**`[物品名]_Sx`** 表示第 x 赛季版本（如 `獠牙_S10`）。历史版本同样用**基名**（去掉 `_Sx`）定位显示图片（`pictures/png/<基名>.png`）。GUI 默认物品池只显示最新版本（`GetLatestOnlyNames()`）；可选物品区与卡组内**右键**可循环切换版本（最新→较高赛季→较低赛季→最新），左键可将当前显示的版本拖入卡组；左键点击其他位置且未拖拽入卡组时，池中已切换到历史版本的项会重置为显示最新版本。**卡组内**版本切换仅在与当前槽位 **MinTier** 一致的版本间循环（避免铜槽切到银版本）。详见 `docs/implementation-notes.md`「多版本物品与 GUI 版本切换」。
- **GUI 卡组集**：启动时优先打开 **上次打开的卡组集**（路径存于 Data/last-collection.txt）；无记录、文件不存在或打开失败时使用 default.json；打开/新建另存为/保存另存为成功时更新记录。详见 implementation-notes「GUI 启动时上次打开的卡组集」。

- **与游戏对齐的物品与测试物品**：`ItemDatabase/CommonSmall.cs`、`CommonMedium.cs`、`CommonLarge.cs` 中已有物品用于对齐当前版本的 The Bazaar，不得为调试/实验随意修改数值、文案或能力行为；如需验证新机制（如 Ability 多触发），应在 `ItemDatabase` 下新建独立文件定义测试物品，并在对应 `RegisterAll` 中**临时**注册，测试通过后移除这些注册语句，避免测试物品进入正式卡组。

- **英雄/关卡专属物品**：海盗等英雄或关卡专属物品**不**放在 CommonSmall/CommonMedium/CommonLarge，应放在 **ItemDatabase/&lt;英雄名&gt;/&lt;尺寸&gt/** 下（如 `Vanessa/small`）。一物一文件，类名与英文名对应（如 `ConcealedDagger.cs`）；工厂方法 **Template()** 表示当前/最新版本，**Template_Sx()** 表示第 x 赛季版本。在对应英雄的 **RegisterAll** 中设置 **db.DefaultHero**（如 `Hero.Vanessa`）、**db.DefaultSize**、**db.DefaultMinTier** 后按档位分批 **db.Register(...)**。详见 `docs/implementation-notes.md`「英雄/关卡专属物品与目录结构」。

- **对战模拟器能力队列**：修改 `BattleSimulator` 中能力队列、施放队列或触发器逻辑时，必须遵守 **.cursor/rules/battle-simulator-ability-queue.mdc**（仅步骤 9 做 next→current、cast 触发的能力入 next、步骤 8 只处理 current、新能力按优先级入 current/next、PendingCount 先 current 再 next）。

- **效果应用上下文**：`IEffectApplyContext` 仅暴露施放者为 **Item**；可选 **InvokeTargetItem** 表示本能力由触发器指向的单一目标触发，效果应对该物品施加（见 implementation-notes「InvokeTarget 与 SameAsInvokeTarget」）。冻结/减速/摧毁等「施加时触发」通过模拟器传入的 **EffectAppliedTriggerQueue** 上报，上下文只追加 `(TriggerName, SideIndex, ItemIndex)`，模拟器在 Apply 后统一 `InvokeTrigger` 并处理 Destroyed。详见 `docs/implementation-notes.md`「EffectApplyContextImpl 化简与触发器统一」。**属性增减**：Reduce 与 Add 统一由 **TargetCondition** 选目标（从双方筛选）；Reduce 己方用 **ReduceAttribute(...).Override(targetCondition: Condition.SameSide)**；日志可 **Override(effectLogName: "解除冻结")**，默认用 **AttributeLogNames**+提高/降低。**不显示某条效果日志**时设 **effectLogName: ""**，实现层在 logName 为空时不调用 OnEffect（见 implementation-notes「步骤 7 施放顺序、Immediate 当场执行与效果日志抑制」）。**冻结/减速/加速/充能**：目标在 Apply 中从**双方**按 **TargetCondition** 筛选（GetTargetsFromBothSides）；Apply 内强制 **NotDestroyed**，减速/加速/充能再强制 **HasCooldown**，冻结不强制 HasCooldown。Override targetCondition 时**不必写** NotDestroyed/HasCooldown。**百分比数值**用 **RatioUtil.PercentOf**（减免量等）或 **RatioUtil.PercentFloor**（至少为 1）。详见 `docs/implementation-notes.md`「属性增减与效果目标选取统一」。**弹药消耗触发器**：用 **Trigger.Ammo**（每次 AmmoRemaining-- 后触发），「仅耗尽当次」用 **additionalCondition: Condition.AmmoDepleted**；「**此物品**弹药耗尽时」须加 **condition: Condition.SameAsSource**。换 trigger 且只传 additionalCondition 时，Override 内会用新 trigger 的默认条件作 base（见 ability-override-format.mdc、implementation-notes「弹药消耗触发器」经验）。弹药物品由注册时 AmmoCap&gt;0 自动获得 **Tag.Ammo**，筛选用 **Condition.WithTag(Tag.Ammo)**；**AmmoDepleted** = WithTag(Tag.Ammo) 且 AmmoRemaining==0。物品描述中「Highest」/「Lowest」指**能力优先级**（AbilityPriority），非目标选取顺序。**左侧/右侧**：仅**相邻**用 **LeftOfSource**/ **RightOfSource**；**所有严格左侧/右侧**用 **StrictlyLeftOfSource**/ **StrictlyRightOfSource**。**己方最左/最右的某类物品**（如最左侧的武器）用 **Condition.LeftMost(Condition inner)** / **Condition.RightMost(Condition inner)**（在满足 inner 的己方物品中取 ItemIndex 最小/最大）；见 `Core/Condition.cs`。**摧毁**：目标可为敌方，**targetCondition: Condition.DifferentSide** 时从敌方选目标。

- **从表格添加物品**：用户以表格图片提供物品时，列顺序为 **Name、所属、版本、minTier、Size、CD、TAG、效果**；第 2 列**所属**填英雄名（如 Vanessa）或**公共**，用于决定放 Hero/size 还是 Common*。效果末尾 (I)/(Hst)/(H)/(L)/(Lst) 表示优先级；英文名决定文件名与类名。**己方最左侧/最右侧的武器**→ LeftMost(WithTag(Weapon)) | RightMost(WithTag(Weapon))；**此物品右侧的武器**→ RightOfSource & WithTag(Weapon)。详见 **.cursor/rules/item-table-convention.mdc** 与 `docs/implementation-notes.md`「从表格添加物品的约定」。
- **源文件长度与拆分**：除 `ItemDatabase/CommonSmall.cs`、`CommonMedium.cs` 等以数据定义为主的文件外，单文件不宜过长。可将嵌套类、静态辅助拆到同命名空间下的独立源文件（例如 BattleSimulator 下的 `EffectApplyContextImpl.cs`、`BattleSideDamage.cs`、`BattleAuraContext.cs`、`TriggerInvokeContext.cs`）。详见 `docs/implementation-notes.md` 中「BattleSimulator 文件拆分与代码结构」。

- **CLI 与测试**：完成任务后用 **CLI** 与物品测试脚本做验证：小型铜 `scripts/run_item_tests_small_bronze.py`、小型银 `scripts/run_item_tests_small_silver.py`、中型铜 `scripts/run_item_tests_medium_bronze.py`、大型铜 `scripts/run_item_tests_large_bronze.py`。测试未通过时在文档中标注「未通过测试」、不放松断言。详见 **.cursor/rules/cli-and-testing.mdc** 与 **docs/cli-and-testing.md**。

- **Git 提交信息**：提交信息须符合 **.cursor/rules/git-commit-format.mdc**（`<type>(<scope>): <subject>`，中文主题，如 `feat(gui): 卡组 MinTier 显示与拖拽`）。
