---
description: Bazaar Arena 项目通用约定（C#、Windows、中文）
alwaysApply: true
---

# 项目约定

- **项目名称**：Bazaar Arena（游戏 The Bazaar 的组队模拟器）。代码与命名空间使用 `BazaarArena`。
- **平台**：面向 Windows；采用 C# 编写。
- **语言**：用户界面文案、代码注释均使用中文。
- **模块与目录**：与设计文档一致，主项目下分为：
  - `DeckManager/` — 卡组管理器
  - `ItemDatabase/` — 物品数据库
  - `BattleSimulator/` — 对战模拟器

命名空间与文件夹对应，例如 `BazaarArena.DeckManager`、`BazaarArena.ItemDatabase`、`BazaarArena.BattleSimulator`。

- **物品定义中的时间**：表示时间时，在物品定义代码中一律使用**秒**作为单位以保证可读性（如 `FreezeSeconds = new[] { 3.0, 4.0, 5.0, 6.0 }`、`Cooldown = 5.0`、`ChargeSeconds = 1.0`）；内部存储可为毫秒，由模板属性做转换（`SecondsOrByTier.ToMilliseconds()`）。

- **物品注册与尺寸/档位**：物品模板中**不**在工厂方法里写 `MinTier`、`Size`；在 **RegisterAll** 中通过 `db.DefaultSize`、`db.DefaultMinTier` 按批次设置，`ItemDatabase.Register` 会将当前默认值写入模板并调用 **EnsureTypeTags**（按属性与 SameAsSource 光环补充护盾/伤害等类型 Tag）后再存入。能力优先使用 **Ability.***：除 **AddAttribute(attributeKey, amountKey?)**、**ReduceAttribute(attributeKey, amountKey?)** 为方法（需指定属性 key）外，其余为**无参属性**（如 `Ability.Damage`、`Ability.Haste`），返回带默认值的 `AbilityDefinition`；定制时统一用 **Ability.xxx.Override(...)** 链式调用，仅 Override 传入的参数会覆盖默认。数值/属性 key 在能力与物品定义侧使用 **Key.Damage**、**Key.Shield**、**Key.Heal**、**Key.Burn**、**Key.Poison**、**Key.Custom_0**（`Core/Key.cs`），替代 `nameof(ItemTemplate.xxx)`。**一个能力一条 Apply**（能力与 Effect 已合并，无 Effects 列表）；默认 trigger 为 UseItem，Condition/TargetCondition 的合并在 **AbilityDefinition.Override** 内完成（将当前定义作为默认与传入的 condition、additionalCondition、targetCondition、additionalTargetCondition 合并）。**AbilityDefinition.Priority** 默认 Medium。**AddAttribute/ReduceAttribute** 时：Condition = 何时触发（引起触发的物品），TargetCondition = 效果施加给谁；文案为「此物品获得 X」时须用 **targetCondition: Condition.SameAsSource**，勿把触发范围（如相邻）误写在 additionalTargetCondition 上。**UseItem 下「当其他某类物品被使用」**（如使用工具时）须用 **condition: Condition.SameSide & Condition.WithTag(Tag.xxx)**，不能用 additionalCondition。**UseSelf**：Trigger 为 UseItem 且未在 Override 中提供 condition 时为 true（仅用 additionalCondition 不变）；仅 UseSelf 能力可参与暴击判定。**暴击**：按物品按帧统一判定（每物品每帧只掷一次，结果存于物品 CritTimeMs/IsCritThisUse/CritDamagePercentThisUse）；Crit 触发生在 ExecuteOneEffect 之前、本帧首次判定为暴击时一次。**Override 的代码风格与参数顺序**见 **.cursor/rules/ability-override-format.mdc**。充能、加速、减速、冻结、修复、摧毁等目标数量在效果层默认 1（GetResolvedValue(..., defaultValue: 1)），物品定义中为 1 时可省略 **ChargeTargetCount**、**HasteTargetCount**、**SlowTargetCount**、**FreezeTargetCount**、**RepairTargetCount**、**DestroyTargetCount**。摧毁用 **Ability.Destroy**（目标仅要求未摧毁、不要求有冷却）；「右侧第一个未摧毁」用 **additionalTargetCondition: Condition.FirstNonDestroyedRightOfSource**。光环用 **AuraDefinition.Value**（Formula?）与 **Percent**（bool）；数值用 **Formula.Source(key)**、**Formula.Opp(key)**、**Formula.Count(condition)** 等组合。若「仅当提供者满足某条件时生效」用 **SourceCondition**（如 **Condition.OnlyCompanion**、**Condition.InFlight**）。需复杂计算时用 **Formula.*** 与 **+/-/***、**一元负号**、**int * Formula** 组合；可用 **RatioUtil.PercentFloor(Formula, percent)** 做百分比。战斗内产生的变量（SideIndex、ItemIndex、Tier、CooldownElapsedMs、InFlight、Destroyed 等）存于 **ItemTemplate** 字典；阵营数值（MaxHp、Hp、Shield、Burn、Poison、Regen）存于 **BattleSide** 字典，均可通过 **GetInt(key)**/名字一致解析。

- **对战模拟器能力队列**：修改 `BattleSimulator` 中能力队列、施放队列或触发器逻辑时，必须遵守 **.cursor/rules/battle-simulator-ability-queue.mdc**（仅步骤 11 做 next→current、cast 触发的能力入 next、步骤 8 只处理 current、新能力按优先级入 current/next、PendingCount 先 current 再 next）。

- **源文件长度与拆分**：除 `ItemDatabase/CommonSmall.cs`、`CommonMedium.cs` 等以数据定义为主的文件外，单文件不宜过长。可将嵌套类、静态辅助拆到同命名空间下的独立源文件（例如 BattleSimulator 下的 `EffectApplyContextImpl.cs`、`BattleSideDamage.cs`、`BattleAuraContext.cs`、`TriggerInvokeContext.cs`）。详见 `docs/implementation-notes.md` 中「BattleSimulator 文件拆分与代码结构」。

- **Git 提交信息**：提交信息须符合 **.cursor/rules/git-commit-format.mdc**（`<type>(<scope>): <subject>`，中文主题，如 `feat(gui): 卡组 MinTier 显示与拖拽`）。
