---
description: 仓库结构、卡组集与 GUI 开发经验总结
alwaysApply: false
globs: "**/*.cs","**/*.xaml","**/docs/**"
---

# 开发经验与仓库约定

## 仓库结构

- **src/BazaarArena/**：主项目（WPF 应用 + 核心逻辑）。子目录与命名空间：`DeckManager/`、`ItemDatabase/`、`BattleSimulator/`、`Core/`。
- **Data/**：静态与默认数据。`Data/Decks/` 为卡组集 JSON 默认目录；`Data/levelups.json` 为等级生命表。启动时若存在 `Data/Decks/default.json` 则自动加载为当前卡组集。
- **docs/**：设计文档、实现笔记、GUI 说明、变更记录。详见 `docs/Bazaar Arena 设计文档.md`、`docs/implementation-notes.md`、`docs/gui-overview.md`、`docs/changelog.md`。
- **.cursor/rules/**：项目约定与开发经验规则，供 AI 与人工查阅。

## 卡组集模型

- **一个 JSON 文件 = 一个卡组集**：格式为 `DeckCollectionFile`（version + decks 数组），每项为 `DeckEntry`（id + deck）。见 `DeckManager/DeckCollectionFile.cs`。
- **DeckManager**：无参构造，维护内存中的 `_decks`、`_order` 与 `_currentCollectionPath`。`OpenCollection(path)` 从文件加载并切换列表；`SaveCollection(path?)` 写回 path 或当前路径；`NewCollection()` 清空内存并清空路径。支持 `Rename`、`MoveUp`、`MoveDown`；列表顺序与 JSON 中 `decks` 数组一致。列表/加载/保存/删除/重命名/移动均针对当前卡组集。
- **自动保存**：保存卡组、另存为卡组、删除卡组、重命名卡组、上移/下移卡组、新建卡组集（选路径后）都会在可能时写回当前卡组集文件；若无路径则弹出“保存卡组集”另存为（初始目录 `App.DecksDirectory` = Data/Decks）。
- **打开/新建卡组集**：文件对话框的 `InitialDirectory` 设为 `App.DecksDirectory`，新建卡组集时用 SaveFileDialog 在 Decks 下创建新 JSON。

## GUI 约定

- **主窗口**：`MainWindow` 为入口，持有 `DeckManager`、卡组列表与编辑区。子窗口（单次模拟、批量模拟）创建时设置 `Owner = this`，以便 `WindowStartupLocation="CenterOwner"` 生效。
- **对话框**：卡组“另存为”等需用户输入的对话框设置 `Owner`，并在 `Loaded` 中通过 `Dispatcher.BeginInvoke(DispatcherPriority.Loaded, () => { TextBox.Focus(); TextBox.SelectAll(); })` 使输入框立即获得焦点，无需再点击。
- **单次模拟**：生命曲线为阶梯状；仅在生命值变化时记录点，且每次变化记录两点 (timeMs, 旧HP)、(timeMs, 新HP)；战斗结束前在 BattleSimulator 中调用 `OnHpSnapshot` 再 `OnResult`，保证最后一帧写入曲线；HP 显示时用 `Math.Max(0, hp)`。

## 对战模拟与日志

- **IBattleLogSink**：模拟器每帧/事件调用 OnFrameStart、OnHpSnapshot、OnCast、OnEffect、OnBurnTick、OnPoisonTick、OnRegenTick、OnResult 等。批量模拟用 `SilentBattleLogSink`；单次模拟用 `StatsCollectingSink` + `TextBoxBattleLogSink` 等组合。
- **StatsCollectingSink**：生命曲线只依赖 `OnHpSnapshot`，在 HP 相对上一记录变化时写入两点形成阶梯；不再按固定间隔写入。详见 `BattleSimulator/StatsCollectingSink.cs`。
- **帧边界**：能力队列使用 current/next 双队列；将 next 移入 current 必须在**本帧所有能力处理完毕之后**，否则会破坏“本帧施放、下一帧生效”的语义。详见 `docs/implementation-notes.md`。

## 物品与数据

- **按等级属性、集合表达式**：物品模板扩展属性用 `_intsByTier`；`IntOrByTier` + `[CollectionBuilder]` + `IEnumerable<int>` 支持 `Damage = [25,35,45,55]` 等写法；`IntOrByTier.Add(delta)` 用于战斗内对每 tier 加值（如举重手套）。详见 `docs/implementation-notes.md`、`csharp-standards.mdc`、**item-design.mdc**。
- **公共物品**：仅保留 `ItemDatabase/Common.cs` 注册公共物品；`App`/`Program` 只调用 `Common.RegisterAll`。新增公共物品在 Common 中写工厂方法并在 `RegisterAll` 中注册。**物品设计经验**（触发器、战斗开始、自定义效果、ValueKey、Tooltip、灼烧衰减等）见 **item-design.mdc**。
- **ItemTemplate.Desc**：物品模板有 `Desc` 属性，可含占位符 `{Damage}`、`{Cooldown}`、`{Burn}`、`{Poison}`、`{Custom_0}` 等；卡组编辑区悬停时替换为当前 tier 或全 tier 数值，并与 `EffectKeywordFormatting` 关键词着色一致。**无冷却（CooldownMs 为 0）时不显示冷却行**。Desc 中可用分号（`;` 或 `；`）分段，Tooltip 会按段拆成多行显示。
- **效果预定义与自定义**：`Core/Effect.cs` 提供 `Effect.Damage`、`Effect.Burn`、`Effect.WeaponDamageBonus(ValueKey: "Custom_0")` 等。预定义效果仅设 `Kind` 与 `ValueKey = EffectKind.XXX.GetDefaultTemplateKey()`；`Kind = Other` 时用 `CustomEffectId` 在 `BattleSimulator/CustomEffectHandlers.cs` 中派发，不在 BattleSimulator 内堆逻辑。
- **避免魔法字符串**：标签用 `Tag.Weapon`、`Tag.Friend` 等（`Core/Tag.cs`）；触发器用 `Trigger.UseItem`、`Trigger.BattleStart`（`Core/Trigger.cs`）；AuraDefinition/Effect 的 AttributeName、ValueKey 等用 `nameof(ItemTemplate.xxx)`。详见 `docs/implementation-notes.md`「避免魔法字符串」。
- **EffectKind 与策略表**：Kind→默认 key、Kind→日志名集中在 `EffectKindKeys` 与 `EffectKindExtensions`；BattleSimulator 中效果应用通过 `GetEffectApplier(EffectKind)` 策略表派发，新增标准效果时在策略表与 EffectKindKeys 中扩展即可。
- **数据与日志路径**：见 `data-and-logging.mdc`（Data/、Logs/、levelups 解析、CopyToOutputDirectory）。

## Tooltip 与悬停说明

- **延后构建防卡顿**：不在 MouseEnter 时构建 Tooltip 内容。卡组内物品：为 Border 设置空 `ToolTip`，在 `ToolTip.Opened` 中根据 `PlacementTarget.Tag`（SlotRowViewModel）再构建并赋 `Content`。物品池：使用 `ToolTipService.ToolTipOpening`，在事件里根据 DataContext（itemName）构建或从缓存取，赋给该元素的 `ToolTip.Content`。
- **冷却行条件**：仅当物品有冷却（`CooldownMs > 0`）时显示「冷却时间：X 秒」行；无冷却物品不显示该行。
- **物品池缓存**：`_poolToolTipCache`（Dictionary<itemName, Border>）缓存已构建的物品池 Tooltip，同一物品多次悬停不重复构建。
- **等级颜色**：Tier 使用标准色（Bronze 180,98,65；Silver 192,192,192；Gold 255,215,0；Diamond 0,255,255），`TierToBrush` 与 `TierToBrushConverter` 一致。Tooltip 背景 `#35322e`，默认文字白色。效果关键词（灼烧、剧毒等）颜色与战斗日志一致，见 `EffectKeywordFormatting`。

## 修改与扩展时注意

- 新增窗口或对话框时记得设置 `Owner`，并在需要时在 Loaded 中聚焦输入控件。
- 修改战斗结算顺序或能力队列移动时机时，对照设计文档的帧顺序与 `implementation-notes.md` 的帧边界说明。
- 卡组集相关逻辑集中在 DeckManager 与 MainWindow 的保存/另存为/删除/重命名/上移下移/新建卡组集中，保持“有路径则写回，无路径则弹窗”的一致性；卡组列表顺序由 DeckManager 的 `_order` 维护，与 JSON 中 `decks` 数组一致。
- 卡组内移除物品：编辑区 StackPanel 设 `AllowDrop`，拖拽卡组内物品到非卡组区域时在 `EditorArea_Drop` 中调用 `RemoveSlotRow`，无需单独“删除”区。
