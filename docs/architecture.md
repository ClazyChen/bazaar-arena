# 架构概览（新栈）

目标分层：

- 前端层（Vue）：只负责 UI 与交互，不进行计算
- 后端层（Flask）：负责 API、SQLite 持久化与展示级聚合；需要计算时调用 `engine/cli`
- 计算层（C++）：对战模拟与搜索等重计算逻辑，输出 JSON
- 数据层（YAML）：人工维护的物品定义源数据
- 格式转换层（Python）：从 YAML 生成 C++ 静态数据与 SQLite 显示库

